# v9.0.0 反映手順（GitHub → タグ → STUDIO）

2026-09-12 ／ PC版トップの全見開きと、嘉左衛門の詳細ページが対象です。SP版は触りません。

---

## 1. 今回のファイルと置き場所

| ファイル | 置き場所 |
|---|---|
| `book.v9.css` | リポジトリ `tukinowa-git/tukimichi-test` の**直下**（book.v8.css と同じ場所） |
| `book.v9.js` | 同上 |
| `kz-logo.svg` | リポジトリの **img/** フォルダ（**新規**） |
| `kz-floor-1f.svg` | 同上（**新規**） |
| `kz-floor-2f.svg` | 同上（**新規**） |
| `p07-tsukuru-main.webp` | 同上（**上書き**。同じ名前のファイルがあります。1200×1448px・142KB の高解像度版です） |
| `claude_PC版embedコード_v9_0_2026-09-12.html` | STUDIO のトップページ → カスタムコード →〈body〉終了タグ直前（丸ごと入れ替え） |
| `claude_嘉左衛門ページembedコード_v9_0_2026-09-12.html` | STUDIO の /stay/kazaemon → カスタムコード →〈body〉終了タグ直前（丸ごと入れ替え） |

古い `book.v8.css`・`book.v8.js` は**消さずに残して**ください。切り戻しに使います。

---

## 2. 反映の順番（この順を守る）

**タグを切る前に STUDIO に貼ると、CSS も JS も画像も存在しないので、ページ全体が出ません。**

### 2-1. GitHub にファイルをアップロード

1. https://github.com/tukinowa-git/tukimichi-test を開きます。
2. まずリポジトリ直下で「**Add file**」→「**Upload files**」。`book.v9.css` と `book.v9.js` をドラッグして Commit します。
3. 次に **img/ フォルダを開いてから**、同じ手順で `kz-logo.svg`・`kz-floor-1f.svg`・`kz-floor-2f.svg`・`p07-tsukuru-main.webp` の4点をドラッグして Commit します。
   `p07-tsukuru-main.webp` は同じ名前のファイルを上書きします。そのまま進めて構いません。

img/ の中に入れないと画像が出ません。アップロード画面の上部に `tukinowa-git / tukimichi-test / img` と出ているか確認してください。

### 2-2. タグ v9.0.0 を作る

1. 右側の「**Releases**」→「**Draft a new release**」。
2. 「Choose a tag」に `v9.0.0` と入力し、「**Create new tag: v9.0.0 on publish**」を選択。
3. Release title に `v9.0.0`、「**Publish release**」。

### 2-3. jsDelivr で確認

次の4本をブラウザで開き、中身が表示されることを確認します。

- https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v9.0.0/book.v9.css
- https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v9.0.0/book.v9.js
- https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v9.0.0/img/kz-logo.svg
- https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v9.0.0/img/kz-floor-1f.svg

CSS と JS の1行目がそれぞれ `/*! book.v9.css` と `/* book.v9.js` で始まっていれば OK です。
「Couldn't find the requested release」と出る場合はタグを切った直後です。数分おいて再読み込みしてください。

### 2-4. STUDIO に貼って公開

1. トップページの「ページ設定」→「カスタムコード」→「〈body〉終了タグ直前」を開き、中身を**すべて削除**して `claude_PC版embedコード_v9_0_2026-09-12.html` を**すべて**貼り付け。文字数は **28,110字**（上限30,000字）です。
2. /stay/kazaemon も同じように `claude_嘉左衛門ページembedコード_v9_0_2026-09-12.html` に入れ替え。
3. **SP用 Embed ボックスは触らずに**残します。
4. 公開。

---

## 3. 反映後に見るところ（チェックリスト）

### トップページ

- [ ] 各ページ左下の「scroll」がサンセリフ（Karla）になっている
- [ ] 施設名の上の英字（guesthouse & cafe など）とリンクの文字が明朝になっている
- [ ] リンクの縦線が、文字の**上**ではなく**右どなり**に立っている
- [ ] **00 ローディング** … これまでどおり
- [ ] **01 目次** … 「私たちのこと」を押すと別タブで tukinowa.jp が開く／枠が文字に合った大きさ／左の説明文が大きく濃い
- [ ] **02 Instagram** … カードの影が濃い
- [ ] **03 とまる** … 住所が施設名の下にある／間取りの列と「詳細を見る」の下がそろっている／平左衛門は「開業準備中」
- [ ] **04 たべる** … 交通の「ＩＣ」「ＪＲ」「１０台」「３０分」が横倒しでなく縦に読める／「ご利用はこちら」で「ためす」へめくれる
- [ ] **05 しる** … ワークショップの写真と文字が重なっていない／ツアーガイドがノドから離れている
- [ ] **06 ためす** … シェアアトリエの写真と文字が重なっていない
- [ ] **07 つくる** … 右の写真がはっきり鮮明になっている（帽子の編み目や袋の織りが見える）／左の写真と文字がノドから離れている
- [ ] **08 すまう** … 下の2枚の写真がノドから離れている
- [ ] **09 お問い合わせ** … 地図が大きく、2枚の間が広い／地図をクリックせずに拡大・移動できる／ロゴが大きい

### 嘉左衛門ページ（/stay/kazaemon）

- [ ] 右上に紋＋題字のロゴが出ている（点線の枠が消えている）
- [ ] 上が **2F**（ルーム②・ルーム③・階段）、下が **1F**（大広間・キッチン・玄関ホール）
- [ ] 「ゲストハウス」の見出しがトップの施設名と同じ書体になっている
- [ ] 情報欄の文字がトップの説明文と同じ大きさになっている
- [ ] 情報欄がノドから離れている
- [ ] とまる見開きの「詳細を見る」から入ったとき、写真の額縁がずれずにつながる

地図の上でマウスホイールを回すと、本はめくれず地図が拡大します。これは意図した動きです。地図の外にカーソルを出せば、これまでどおりめくれます。

---

## 4. うまくいかないときの切り戻し

STUDIO の埋め込みコードを v8.0 のものに戻して公開するだけで、元に戻ります。GitHub のファイルもタグもそのまま残しておいて構いません。

---

## 5. 積み残し

### 5-1. まだ写真が無い6スロット

`p05-shiru-xs` ／ `p05-shiru-sub1` ／ `p06-tamesu-xs` ／ `p08-sumau-xs` ／ `p08-sumau-sub1` ／ `p08-sumau-sub2`

グレー表示のままです。撮影ができたら、同じ名前で img/ に上書き → 新しいタグ → 埋め込みコードの `v9.0.0` を新しい番号に書き換え、の3手順で差し替えられます。

### 5-2. まだ決まっていないリンク先

`#about`（廃止済み）以外に、次のリンクがダミーのままです。行き先が決まったら書き換えてください。

`#plan`（体験プラン）／`#kitchen-rule`（利用規約）／`#kitchen-flyer`（募集チラシ）／`#kanaya`（商品を見る）／`#fabel`（紹介サイト）／`#nariwai`／`#concierge`、および Instagram・LINE の各アカウントURL。

### 5-3. お問い合わせフォーム

`window.TK_FORM.url`（GASのURL）と `pp`（プライバシーポリシーのURL）は空のままです。埋め込みコードの末尾にあります。ここを書き換えて STUDIO で公開するだけで反映され、GitHub のタグを切り直す必要はありません。

---

## 6. 検証した内容

- Chromium で **1920×1080 / 1680×1050 / 1600×900 / 1440×900 / 1400×868 / 1280×800 / 1152×720 / 1024×680** の8サイズを描画確認。目次の説明文がどのサイズでも矢印を越えないこと、地図の帯が「お問い合わせ」の縦見出しに当たらないこと、奥付のロゴが枠からはみ出さないことを自動チェック。
- 写真と文字の重なりを座標で確認（しる：30px空き／ためす：23px空き／いずれも v8 は重なり）。
- 「ご利用はこちら」を実際にクリックして、ためすの見開き（6）に移ることを確認。
- 「私たちのこと」の `href` / `target` / `rel` を確認。
- 地図の iframe が `pointer-events:auto` で読み込まれ、「クリックで操作」の表示が消えていることを確認。
- `book.v9.js` を Node で構文チェック、`book.v9.css` の中括弧の対応を確認。
- SVG3点は SVGO 最適化の前後で描画差が無いこと（平均差 0.08/255 以下）を画素比較で確認。
- つくるのメイン画像を高解像度版（1200×1448px）に差し替え、1920px幅の実表示で旧版と並べて比較（表示サイズ換算での鮮鋭度は約3.4倍）。
- 設計データ（1400×868）と照合：間取り図の枠（2F 271×153px／1F 309×180px）、ロゴの位置（右ページ右から12.71%・上から29.61%・高さ209px）、「私たちのこと」の枠（高さ172px）、リンクの縦線の位置と丈。
