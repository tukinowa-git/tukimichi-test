# 月みちて、山となる。 v11.4 反映手順と検証結果（2026-09-17）

v11.3.0 の確認後にいただいた修正指示（5回目）を反映した版です。
今回は画像の追加・差し替えはありません。

## 1. 納品物

| ファイル | 置き場所 | 内容 |
|---|---|---|
| `book.v11.4.css` | GitHub リポジトリ直下 | スタイル本体 |
| `book.v11.4.js` | GitHub リポジトリ直下 | エンジン本体（戻り時の処理とInstagram取得の開始時期を追加） |
| `claude_PC版embedコード_v11_4_2026-09-17.html` | STUDIO トップのカスタムコード | 26,621字（残り3,379字） |
| `claude_嘉左衛門ページembedコード_v11_4_2026-09-17.html` | STUDIO /stay/kazaemon のカスタムコード | 11,084字 |

## 2. 反映手順

1. `book.v11.4.css` と `book.v11.4.js` をリポジトリ直下にアップロードし、Commit します。
2. Releases → Create a new release → タグ `v11.4.0` → Publish。
3. `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.4.0/book.v11.4.css` が開けることを確認します。
4. トップと /stay/kazaemon の両方に、新しい埋め込みコードを貼って公開します。

元に戻すときは、v11.3 の埋め込みコード2本を貼り直すだけです。

## 3. 詳細ページとの行き来が遅い件

### 3-1. 原因（実測）

**往路（トップ → 嘉左衛門）**
リンクにカーソルを合わせた時点で、HTML だけは先読みしていました。しかし**詳細ページだけが使う素材は先読みされておらず**、移動してから取りに行っていました。

| 素材 | 容量 |
|---|---|
| stay-kazaemon-xs.webp（外観写真） | 250KB |
| kz-floor-1f.svg / kz-floor-2f.svg（平面図） | 48KB |
| kz-logo.svg（題字） | 22KB |
| **合計** | **約321KB** |

CSS・背景・アイコン・フォントはトップと共通で、キャッシュから読まれます。つまり待ち時間のほとんどは、この321KBと、STUDIO のページ自体の描画でした。

**復路（嘉左衛門 → トップ）**
毎回トップを**新規に読み込み直していました**。読み直すものは次のとおりです。

- 画像25枚（合計 約1,796KB。キャッシュがあっても展開の時間はかかります）
- GSAP 72KB ＋ エンジン 36KB ＋ CSS 70KB の解析と実行
- めくりエンジンの組み立て、到着演出（最大1.4秒）

戻るときは、直前に見ていたトップがそのまま残っているのに、それを捨てて作り直していたことになります。ここが体感の遅さの主因でした。

**共通**
リンクを押してから移動するまでに、送り出しの演出で 190ms を使っています（意図した演出です）。

### 3-2. 対策（v11.4 で入れたもの）

**① 詳細ページの素材を先に取っておく（すべてのブラウザ）**
とまる（見開き3）に着いた時点で、上の321KBを裏で読み込みます。読み込みが落ち着いた0.7秒後に始めるので、表示の邪魔はしません。
詳細ページへ移ったときには、写真も平面図も手元にある状態になります。

**② 詳細ページを先に組み立てておく（Chrome など）**
リンクに近づいた時点の動作を、「HTMLだけ先読み」から「ページを丸ごと先に組み立てておく」方式（prerender）に変えました。クリックした瞬間に表示されます。
対応していないブラウザ（Safari など）では何も起きず、①の先読みだけが効きます。
元に戻したいときは、埋め込みコードの `"prerender"` を `"prefetch"` に書き換えてください（1語だけです）。

**③ 戻りは「読み直さない」（すべてのブラウザ）**
詳細ページのナビからトップへ戻るとき、**トップから来ている場合だけ**ブラウザの「戻る」を使います。ブラウザの再表示キャッシュ（bfcache）にトップが残っていれば、読み込み直さずに一瞬で復元されます。GSAP もエンジンも画像も、作り直しがありません。

**④ 戻ったあと、目的の見開きまでめくる**
「戻る」で復元されたトップは、出ていったときの見開き（とまる）のままです。ナビで別の見開き（たべる など）を選んでいた場合は、復元後にそこまでめくります（`book.v11.4.js` の㉔）。

**⑤ Instagram の取得を後ろにずらした**
これまでは表示開始と同時に取得していました。v11.4 では、まず控え（前回の内容）を表示し、通信はページの読み込みが落ち着いてから始めます。最初の表示と画像の取得に帯域を譲るためで、見た目の動きは変わりません。

### 3-3. 安全策（うまくいかない環境でも壊れないように）

- **「戻る」が使えないとき**（トップ以外から直接開いた、再表示キャッシュが効かない、など）は、これまでどおり `/` へ移動します。移動先の見開きの指定は従来と同じ仕組みで渡しているので、**結果は同じ**です。
- 「戻る」を試して 0.7秒以内に何も起こらなかった場合も、自動でふつうの移動に切り替えます。
- ②の prerender は、対応していないブラウザでは何も起きません。取り消しも1語です。
- ①の先読みは、とまるに着くまで始まりません。詳細ページを見ない方の負担を増やさないためです。

### 3-4. 検証したこと・できなかったこと

ローカルに `/` と `/stay/kazaemon` を並べ、実際にリンクをたどって往復させました。

- とまるに着いた時点で、321KBの4ファイルを取りに行くことを確認しました。
- トップ → 詳細 → ナビで「たべる」を選択 → **トップに戻り、見開き4が開く**ことを確認しました。送り出しの表示が残ることもありません。
- 詳細ページを直接開いた場合（トップから来ていない場合）は、これまでどおりの移動になることを確認しました。
- **再表示キャッシュ（bfcache）そのものは、この検証環境では確認できませんでした。** 自動操作中のブラウザでは無効化されるためです。検証環境では毎回「読み直し」の経路を通りましたが、その場合でも結果は同じ（見開き4が開く）でした。実機では Safari・Chrome とも再表示キャッシュが働くため、戻りはさらに速くなります。

### 3-5. 今回は見送った案

- **CSSをページごとに分ける**：詳細ページは全10見開き分の CSS（70KB）を読んでいますが、トップと同じファイルのためキャッシュが効きます。分けると版管理が二重になり、崩れの危険が大きいので見送りました。
- **画像をさらに軽くする**：しるのメイン写真（283KB）などは、画質を少し下げれば100KB程度減らせます。画像の再アップロードが必要なので、ご希望があれば次回に含めます。
- **演出の時間短縮**：送り出し 190ms・到着 1.4秒は意図した演出のため、そのままにしています。短くする場合は `--lvD` `--arD` で調整できます。

## 4. そのほかの修正

### 07 つくる
叶屋「商品を見る」のリンク先を `https://cskanaya.official.ec` にしました（別タブで開きます）。

### 09 お問い合わせ
「※必須」の色を、赤みの強い `#c62a1c` にしました（v11.3 は `#c0392b`）。入力エラー時の色は変えていません。

### 嘉左衛門ページ

**本文**
ご指示の文面に差し替えました。1列14字・両端揃えのままで、12列に組まれます。

**ひとつ確認をお願いします。** ご指示の文では「代々引き継がれきた」となっていましたが、「て」が抜けているとみて「代々引き継がれてきた」にしています。そのままの表記にする場合はお知らせください。

**「宿泊プランを見る」の下位置**
右どなりの段落（◎アクセス）の最終行と、ぴったり同じ高さにそろえました。
ずれていた理由は、この段落のいちばん長い行が16字で、桁（17字）より1字ぶん短いためです。リンクは桁の下端に合わせて置かれていたので、1字ぶん下に出ていました。その1字ぶん（1.02em＋1px）だけ持ち上げています。

## 5. 検証結果

1400×868／1512×870／1280×720／1920×1080 で確認しました。

- **嘉左衛門**：「宿泊プランを見る」と右どなりの段落の下端の差は、4サイズすべてで 0.0px です。
- **「※必須」**：`rgb(198, 42, 28)` になっています。
- **商品を見る**：リンク先・別タブ・`rel="noopener"` を確認しました。
- **動いていないことの確認**：1400×868 と 1512×870 で v11.3 と比べ、ご指示の箇所以外で動いたものはありません（見出し・本文・写真・情報ブロック・フォーム・平面図・情報欄）。
- **スクリプト**：埋め込みコード内のすべてのスクリプトと `book.v11.4.js` の構文を確認しました。読み込み中のエラーもありません。

## 6. 持ち越し

- シェアアトリエの説明は「縫製機器の貸出し」のままです（チャレンジキッチンのみ「貸し出し」に変更）。
- 目次のしるの説明は「ツアーガイドとワークショップ」のままです。
