# PC版 v4 への差し替え手順

作成日：2026年9月6日／対象：月みちて、山となる。 PC版トップ
前提：CSS・JSはGitHub（`tukinowa-git/tukimichi-test`）、HTMLはSTUDIOの**ページ設定**→カスタムコード→`</body>`直前

---

## 0. 何が変わるか（5行）

1. **文字が濃く・大きくなります。** 提示デザインを画素単位で実測し、本文15px／行間2.13、情報ブロック13px／行間2.0 など、すべて設計データの実寸に合わせました。
2. **左ナビ（目次タブ）が白背景・文字22.8pxになります。** 設計データのナビ背景を測ったところ純白（#ffffff）でしたので、ご指示どおりで設計とも一致します。
3. **ローディング前の「他ページのチラ見え」が止まります。** 原因が特定できたので、CSSとJSの二重で塞ぎました（詳細は §4）。
4. **お問い合わせのタイトルとフォームの重なりが解消し、内容欄が縦に伸びて下の余白を埋めます。**
5. **奥付の嘉左衛門・平左衛門がGoogleマップの埋め込みになります。**

---

## 1. 差し替えるファイル

| ファイル | 置き場所 | やること |
|---|---|---|
| `book.v4.css`（25,092字） | GitHub `tukimichi-test` リポジトリ直下 | **新規アップロード**（v1〜v3は消さずに残す） |
| `book.v4.js`（13,673字） | 同上 | **新規アップロード**（v1〜v3は消さずに残す） |
| リリースタグ `v4.0.0` | GitHub → Releases | **新規作成**（jsDelivrの副系用） |
| `claude_PC版embedコード_v4_2026-09-06.html`（24,227字） | STUDIO →**ページ設定**→ カスタムコード →`</body>`直前 | **中身を全部置き換え** |

> 上限30,000字に対し24,227字なので余裕があります（残り5,773字）。
> 古いものを何ひとつ消さないのがこの構成の要点です。うまく動かなければ、埋め込みコードの `v4` を `v3` に書き戻すだけで元に戻せます。
> **SP用Embedボックスは絶対に削除しないでください。** スマホはSP版が担当します。

---

## 2. 手順

### ステップ1：GitHubに2ファイルを追加

1. `https://github.com/tukinowa-git/tukimichi-test` を開く
2. 「Add file」→「Upload files」
3. `book.v4.css` と `book.v4.js` をドラッグ＆ドロップ（ファイル名が1文字でも違うと読み込めません）
4. 「Commit directly to the `main` branch」を選んで「Commit changes」

### ステップ1-2：リリースタグ `v4.0.0` を作る

`https://github.com/tukinowa-git/tukimichi-test/releases/new` を開き、「Choose a tag」に `v4.0.0` と入力 →「**Create new tag: v4.0.0 on publish**」の行をクリック →「Publish release」。

この行をクリックするまでタグは作られません。ボタンの表示が `v4.0.0` に変われば成功です。

**確認**：下の4つをブラウザで直接開いて、コードが文字として表示されればOKです。

```
https://tukinowa-git.github.io/tukimichi-test/book.v4.css
https://tukinowa-git.github.io/tukimichi-test/book.v4.js
https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v4.0.0/book.v4.css
https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v4.0.0/book.v4.js
```

> GitHub Pagesは最大3分、jsDelivrはタグ作成後1〜2分かかります。404が出たら強制リロード（`Ctrl+Shift+R` / `⌘+Shift+R`）を挟んで待ってください。

### ステップ2：STUDIOの埋め込みコードを入れ替え

1. STUDIO →**該当ページの**ページ設定 → カスタムコード →`</body>`直前のブロックを開く
2. 中身を**全選択して削除**
3. `claude_PC版embedコード_v4_2026-09-06.html` の中身を**全部**貼り付け
4. 保存 →「公開」

### ステップ3：確認チェックリスト

- [ ] **ページを開いた瞬間、ローディング画面以外が一瞬も映らない**（今回の主目的）
- [ ] ローディングのタイトルロゴが大きくなり、中央のイラストに寄っている
- [ ] 左上のコピー文もイラスト寄りになっている
- [ ] 目次の6項目が設計どおりの高さ・間隔で並ぶ
- [ ] Instagramの「日々のあれこれ」が大きくなり、カードが縦長でキャプションが4行出る
- [ ] とまる以降、左ナビの背景が白・文字が大きい
- [ ] 各ページの文字が以前より濃い
- [ ] お問い合わせのタイトルと入力欄が重なっていない
- [ ] 内容欄が下まで伸びて、右ページに余計な余白がない
- [ ] 奥付にGoogleマップが2つ表示され、**地図の上でホイールを回してもページがめくれる**（クリックすると地図操作に切り替わる）
- [ ] 奥付の「運営者：ツキノワ合同会社」が左端で切れていない
- [ ] **ブラウザ幅を768px以下に縮めるとPC版の本が消え、SP版が出る**

---

## 3. 実測にもとづく主な数値（設計データ 1400×868 を画素解析）

| 要素 | 設計の実測 | v3 | v4 |
|---|---|---|---|
| 左ナビ幅 | 96px（罫線 x=95） | 86.8px | 96px（6.86vw） |
| 左ナビ文字 | 22.8px | 18.9px | 22.8px（1.63vw） |
| 左ナビ背景 | #ffffff | #f4f2eb | #ffffff |
| ローディング タイトルロゴ | 300×455px／左45.6%／下1.8% | 154×233px | 301×455px／左45.6%／下1.8% |
| ローディング コピー文 | 16.5px／右端x209 | 13.0px／右端x175 | 16.5px／右端x216 |
| 目次 区切り線 | y220〜690 | y191〜712 | y220〜690 |
| 目次 桁ピッチ | 83.7px | 85.4px | 83.7px |
| Instagram 見出し | 20.7px | 20.0px | 26.0px（※ご指示により設計より大きく） |
| テーマ本文 | 15px／行間2.13 | 13.2px／行間1.72 | 15.0px／行間2.13 |
| テーマ見出し | 33.5px | 30.0px | 33.5px |
| リード | 23.5px／右13.9% | 23.0px／右16.4% | 23.5px／右13.9% |
| 情報ブロック説明 | 13px／行間2.0 | 11.1px／行間1.85 | 13.0px／行間2.0 |
| フォーム入力欄 | x835〜1270 | x777〜1335（タイトルと重なる） | x830〜1270 |

画像の座標も全ページ実測し直しています。とくに**ノドをまたぐ大判画像の左端**は、v3の値が実測より3〜4%左にずれていました（例：とまる 37.23%→41.21%）。

---

## 4. 「他ページのチラ見え」の原因と対策

**原因**：`book.v3.js` が紙9枚（p0〜p8）の重なり順を設定するより前に、ブラウザはDOMの並び順で描画します。その状態では**いちばん最後の p8（すまう左／お問い合わせ右）が最前面**に来ます。`#shield` は「ロゴ画像が読めた時」または「1.5秒後」に消える設計だったため、GSAPの到着が遅いと隙間が生まれていました。

**対策（三重）**

1. **CSSで先に重なり順を確定**（`#p0{z-index:90}` 〜 `#p8{z-index:10}`）。JSが動く前から正しい順序になります。
2. **`.m.pre` で起動までローディング以外を隠す**。`book.v4.js` が紙の配置を終えた瞬間に `pre` が外れます。この2行は**埋め込みコードのインライン`<style>`にも入れて**あるので、外部CSSがまだ届いていなくても効きます。
3. **シールドを外す条件を「ロゴ画像が読めた」かつ「エンジンが起動した」の両方に変更**。どちらかが遅れても最長9秒で必ず先へ進むので、真っ白なまま止まることはありません。

---

## 5. Googleマップについて（要ご確認）

- **嘉左衛門**：ご提供いただいた `pb=` 付きのURLを**そのまま**使っています。
- **平左衛門**：`pb=` パラメータは座標が符号化されたもので推測して作れないため、**キー不要の `?q=...&output=embed` 形式で仮置き**しています。表示はされますが、嘉左衛門と同じ「地図を埋め込む」から取得したURLに差し替えるのがおすすめです。

**差し替え方**：Googleマップで平左衛門を検索 →「共有」→「地図を埋め込む」→ HTMLをコピー → 埋め込みコード内の

```
title="平左衛門の地図" ... src="https://maps.google.com/maps?q=...&amp;output=embed"
```

の `src="..."` の中身だけを、コピーしたiframeの `src` の値に置き換えてください。**`&` は `&amp;` に書き換える**必要はありません（GoogleマップのURLをそのまま貼って構いません）。

**地図の操作について**：地図の上でホイールを回すとページがめくれなくなるのを避けるため、**クリックするまで地図は操作を受け付けません**。右上に「クリックで操作」と出ます。マウスを地図から離すと自動で元に戻ります。

---

## 6. あとから微調整したくなったら

埋め込みコード冒頭の `<style>` にある**CSS変数**を書き換えるだけで、STUDIOのプレビューに即反映されます（GitHubを触る必要はありません）。

```
--i    文字の基本色（数字が小さいほど濃い）
--im   本文の色      --il  見出し脇の小さい文字   --if  いちばん薄い文字
--navW 左ナビの幅     --navFS 左ナビの文字サイズ
--titleW / --titleX / --titleY   ローディングのタイトルロゴ（幅・左位置・下位置）
--introX / --introY / --introFS  ローディングのコピー文
--igtFS  「日々のあれこれ」の文字サイズ
--bdFS   テーマ見開きの本文       --ibdFS  情報ブロックの説明文
```

たとえば「タイトルロゴをもう少し小さく」なら `--titleW:43%` を `40%` に、
「もう少し左（イラスト寄り）へ」なら `--titleX:45.6%` を `43%` に。

---

## 7. 今回は手を入れていない箇所

- **中央のロゴイラストの大きさ**：ご指示になかったため、v3から微増（`--logoW:min(58vw,104vh)`）にとどめています。なお設計データのイラストは縦横比が素材（1.449）より縦長（約1.38）で描かれており、**素材そのままでは設計と完全には一致しません**。合わせ込む場合は素材の切り出しをご相談ください。
- **差し替え待ちの写真18か所**：`class="ph tmp"` / `class="xs tmp"` のまま、グレー＋ラベル表示です。差し替え方はv3の手順書と同じで、`data-i` を書き換えて `tmp` を消します。
- **前回お伝えした表記ゆれ**（とまるの「大広間１室室」、たべるの「スイーツ提供する」、つくる／叶屋の文末、平左衛門の住所）：v3のまま暫定処理しています。
- **GASのお問い合わせ送信先**：`book.v4.js` の `GAS_URL` は空のままです。
- **助成ロゴ**：枠だけの仮表示（`<div class="fgrl">助成<br>ロゴ</div>`）のままです。

---

## 8. 困ったときは

| 症状 | 考えられる原因 | 確認すること |
|---|---|---|
| レイアウトが崩れる | CSSが届いていない | `book.v4.css` のURLを直接開いて確認 |
| ローディングのまま進まない | JSかGSAPが読めていない | `book.v4.js` のURLを直接開く。検証→Consoleでエラー確認 |
| ナビを押しても動かない | 旧版のJSが残っている | 読み込み先が `book.v4.js` になっているか確認 |
| 地図が真っ白 | 埋め込みURLの誤り | `src` をブラウザのアドレス欄に貼って表示されるか確認 |
| 他ページがまだチラつく | 埋め込みの `class="m ld pre"` から `pre` が抜けている | 埋め込みコードを貼り直す |
| スマホでPC版が出る | `@media(max-width:768px){.m{display:none}}` が消えている | 768の値を、embed・`book.v4.js` の `NARROW()`・StudioのSP用Embed表示切替の3か所で揃える |
