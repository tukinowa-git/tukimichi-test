# 月みちて、山となる。 v11.5 反映手順と検証結果（2026-09-17）

v11.4.0 で出た「詳細ページから戻るとめくって進んでしまう」不具合の修正と、表示速度の追加対策です。
画像の追加・差し替えはありません。

## 1. 納品物

| ファイル | 置き場所 | 内容 |
|---|---|---|
| `book.v11.5.css` | GitHub リポジトリ直下 | スタイル本体（中身の変更はありません。版の表記のみ） |
| `book.v11.5.js` | GitHub リポジトリ直下 | エンジン本体（不具合の修正） |
| `claude_PC版embedコード_v11_5_2026-09-17.html` | STUDIO トップのカスタムコード | 27,502字（残り2,498字） |
| `claude_嘉左衛門ページembedコード_v11_5_2026-09-17.html` | STUDIO /stay/kazaemon のカスタムコード | 11,253字 |

## 2. 反映手順

1. `book.v11.5.css` と `book.v11.5.js` をリポジトリ直下にアップロードし、Commit します。
2. Releases → Create a new release → タグ `v11.5.0` → Publish。
3. `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.5.0/book.v11.5.css` が開けることを確認します。
4. トップと /stay/kazaemon の両方に、新しい埋め込みコードを貼って公開します。

元に戻すときは、v11.4 の埋め込みコード2本を貼り直すだけです。

## 3. 不具合の原因と修正

### 3-1. 原因

v11.4 で、詳細ページから戻ったときに「行き先の見開きまで移動する」処理を足しました。このとき呼んでいたのが、**メニュー用のめくり関数（`go`）**でした。この関数は名前のとおり、目的の見開きまで**1枚ずつめくって進む**ものです。

そのため、とまるの位置で残っていたトップが復元されたあと、とまる → たべる → … → すまう と、めくりながら進む動きになっていました。

**1回目だけ直接表示されたのはなぜか**
1回目は、トップがまだブラウザの再表示キャッシュ（bfcache）に入っていないことが多く、「読み込み直し」の経路を通ります。この経路では起動時に行き先を読んで**一発で目的の見開きを開く**ため、正しく見えていました。
2回目以降は再表示キャッシュが効くようになり、めくる経路に入っていた、という順序です。

### 3-2. 修正

戻ったとき専用に、**めくらずに一瞬で移る処理（`warp`）**を作りました（`book.v11.5.js` の㉖）。起動時のディープリンクと同じ「めくり終わった状態」を、アニメーションなしで組み立てます。

処理の順番も変えました。

1. 復元された時点では、出ていったときの状態（中身が隠れている）のまま
2. **隠れているうちに**、目的の見開きへ移動
3. そのあとで中身を出す

これにより、途中の見開き（とまる）が一瞬見えることもありません。

**メニューやホイールでの通常のめくりは、これまでどおり1枚ずつめくる動きのままです。** 変えたのは「戻ってきたときだけ」です。

### 3-3. 検証

- 復元を再現した試験で、**60ms以内**に目的の見開き（すまう）へ移動し、紙の角度も最終状態、めくり影の残りもゼロでした。表示は隠れた状態から、目的の見開きで現れます。
- ナビの印（すまう）、情報ブロックの表示も正しく切り替わりました。
- 通常のメニュー操作（3→6）は、従来どおり1枚ずつめくる動きのままです（5.4秒）。
- 実際にリンクをたどる往復でも、詳細ページ →「すまう」→ トップの見開き8、と直接移動しました。

## 4. 表示速度について（追加の分析と対策）

### 4-1. 分かったこと：待ち時間の主因は STUDIO 側のページ生成

サイトの応答ヘッダーを実測しました。

- **`cache-control: private, no-store`**
  STUDIO が返す HTML は、**ブラウザに保存してはいけない**指定になっています。つまり v11.3 以前から入れていた「HTMLの先読み（prefetch）」は、**原理的にほとんど効きません**。取ってきても捨てられるためです。
- **`server-timing: fetchSiteData;dur=470〜1403ms`**
  1ページの HTML を作るのに、STUDIO 側で **0.5〜1.6秒**かかっています（計測はパスワード画面での値です。中身のあるページではこれ以上かかる可能性があります）。

移動のたびにこの時間が必ず発生していたため、v11.4 の改善が体感しづらかったと考えられます。

### 4-2. v11.5 で行った対策

**① 事前描画（prerender）の開始を前倒し**
ページを丸ごと先に組み立てておく指示を、「リンクにカーソルを合わせた時」から「**見開き2（Instagram）に来た時点**」に変更しました。
カーソルを合わせてから押すまでは0.2秒ほどしかなく、1.5秒の生成には間に合いません。見開き2からなら、とまるを読んでいる間に生成が終わります。
この仕組みは、**待ち時間を消せる唯一の方法**です（HTMLを保存できないため）。

**② 対応していないブラウザ向けの温め**
事前描画に対応していないブラウザ（Safari など）では、同じタイミングで HTML を一度取りに行きます。ブラウザには保存されませんが、STUDIO 側の生成結果が温まれば、次の本番の要求が速くなる可能性があります。

**③ 画像の先読みも見開き2から**
詳細ページだけが使う321KBの先読みを、見開き3から見開き2に前倒ししました。

**④ 切り替えの演出を短く**
「表示の切り替えは短い時間で」というご要望に合わせ、次のように詰めました。

| 項目 | v11.4 | v11.5 |
|---|---|---|
| 送り出しの待ち時間 | 190ms | 130ms |
| 送り出しの淡い消え方（--lvD） | .19秒 | .13秒 |
| 到着の淡い現れ方（--arD） | .5秒 | .36秒 |

戻す場合は、埋め込みコードの `--lvD` `--arD` の値を戻してください。

### 4-3. まだ残っている制約と、次の一手

- **ブラウザによって効き方が違います。** Chrome 系は①が効き、体感はかなり変わるはずです。Safari は事前描画にも先読みにも対応していないため、効くのは③④だけです。
  **どのブラウザで確認されているか**を教えていただければ、そこに合わせた手を考えます。
- **最終手段としては、詳細ページを別ページにしない案**があります。嘉左衛門の内容を本の見開きの一枚として持てば、ページ移動そのものが無くなり、めくりと同じ速さで表示できます。ただし現在の構成（別URLで検索に載せる方針）を変えることになるため、ご相談のうえで判断したい部分です。
- **画像をさらに軽くする案**も残っています。しるのメイン写真（283KB）などを再変換すれば、トップ全体で100〜200KB減らせます。

## 5. 検証結果

- 復元後の移動：60ms以内に完了、途中の見開きは表示されません。
- 通常のめくり：従来どおりの動きです。
- 見開き2で、画像4点の先読みと事前描画の指示が出ることを確認しました。
- リンクをたどる往復（トップ → 詳細 → すまう）が正しく動作し、読み込み中のエラーはありません。
- **レイアウトは v11.4 と完全に同一**です（トップの見開き3・5・7・9と、嘉左衛門ページの情報欄・本文・平面図・リンクを比較）。

## 6. 持ち越し

- シェアアトリエの説明は「縫製機器の貸出し」のままです。
- 目次のしるの説明は「ツアーガイドとワークショップ」のままです。
- 嘉左衛門の本文は「代々引き継がれてきた」としています（ご指示の文では「て」が抜けていました）。
