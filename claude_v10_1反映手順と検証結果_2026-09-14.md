# v10.1 反映手順と検証結果（2026-09-14）

## 1. 反映の順番

1. GitHub リポジトリ直下に **`book.v10.1.css`** と **`book.v10.1.js`** をアップロード
   - **画像の追加はありません。** v9.0.0 のものをそのまま使います
2. Releases → Create a new release → タグ **`v10.1.0`** → Publish
3. ブラウザで開けることを確認
   `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v10.1.0/book.v10.1.css`
4. STUDIO に2本とも貼り替えて公開
   - トップページ … `claude_PC版embedコード_v10_1_2026-09-14.html`
   - `/stay/kazaemon` … `claude_嘉左衛門ページembedコード_v10_1_2026-09-14.html`

2 を飛ばして 4 をやると、タグが存在しないので全部出ません。
トップと嘉左衛門は同じタグを見ているので、必ず2本セットで貼り替えてください。

**v10.0.0 のタグとファイルは消さずに残しておいて構いません。** v10.1 の表示には
何の影響もありません。

STUDIO の3万字制限に対する余裕：トップ 1,678字 ／ 嘉左衛門 19,053字

---

## 2. v10.0 が壊れた原因（確定）

### 症状
- Chrome でノド（見開きの境）に1本の線が見える
- 全ページでリンクが触れない

### 原因はひとつ。Safari 対策で紙9枚に 0.08px の奥行きを与えたことです。

**線** — ノドのちょうど700px列の明るさを測ると、v9が `106` に対し v10.0 は `149`。
1ピクセルだけ明るい筋が出ていました。紙に奥行きを与えたことで、紙の縁が
ピクセル格子に乗らなくなり、ノドに隙間ができていました。

**リンク** — 「詳細を見る」の中心でクリック判定を取ると、v9 は `OK`、
v10.0 は `BLOCKED by pf`。奥にいるはずの紙が、クリック判定の上では手前に
来ていました。奥行きの差が小さすぎて Chrome が「同じ平面」と見なし、
塗りは z-index どおりなのに、判定だけ DOM 順になっていたためです。

### 対策
**この方式はすべて取り消しました。** `book.v10.1.js` の本文は
`book.v9.js` と1バイトも違いません（差分比較で確認済み）。
版番号を CSS とそろえるためだけのファイル名変更です。

v10.1 で線が消えていること、リンクが反応することは、どちらも実測で確認しています
（ノド700px列：v10.1 は `106` で v9 と同じ／リンク79個すべて v9 と同じく反応）。

---

## 3. 今回のやり方 — なぜ位置が動かないのか

情報ブロックは `row-reverse` ＋ `flex-start` なので、中身は**右づめ**で並びます。
つまり `left` は見た目の位置ではなく、「ここより左へは出ない」という**上限**です。

```
.k1L .ibs{left:0;right:22%;top:68.2%;height:25.5%}
           ↑変えた   ↑v9のまま  ↑v9のまま  ↑v9のまま
```

`left` を 0 に寄せても**中身は1pxも動きません**。段落間を広げるための幅は、
すべてここから出しています。

その結果、**ノド側の余白・上下位置・高さ・写真・見出しの位置は v9 のまま**、
一切触っていません。v9 との画像差分も意図した箇所だけに収まりました。

| 見開き | 左ページの差分 | 右ページの差分 |
|---|---|---|
| 01 目次 | 14,657px（目次の文字・キャプション） | **0px** |
| 02 Instagram | 1,094px | 2,160px（どちらも y46-270 のみ） |
| 03〜08 | 17,000〜27,000px（段落間） | 約5,000px（見出しとリード） |
| 09 お問い合わせ | 37,333px（※下記④参照） | **0px** |

---

## 4. 検証結果（v9 と同一のチェッカーで突き合わせ）

15通りの画面サイズ × 全10見開き＋嘉左衛門ページ。

| 項目 | v9 | v10.1 | |
|---|---|---|---|
| 目次のボタン食い込み | 54件 | **0件** | 解消 |
| 目次本文の折り返し | 54件 | **0件** | 解消 |
| 嘉左衛門「設備」の変な改行 | 15件 | **0件** | 解消 |
| 嘉左衛門「アクセス」の改行 | 1件 | **0件** | 解消 |
| 嘉左衛門 情報欄のはみ出し | 2件 | **0件** | 解消 |
| 目次キャプションのはみ出し | 36件 | 36件 | v9からの持ち越し（悪化なし） |
| 情報ブロックのはみ出し | 0件 | 0件 | |
| リンクのクリック判定 | 79個OK | 79個OK | |

**v9 より悪化した項目はひとつもありません。**

検証したサイズ：1920×1080 / 1680×1050 / 1600×900 / 1512×900 / 1440×900 /
1440×820 / 1400×868 / 1366×768 / 1280×800 / 1280×720 / 1180×820 /
1100×700 / 1024×640 / 900×600 / 820×560

---

## 5. 目次の重なりについて

最長行24文字 × 1.04em = **24.96em** 必要なのに対し、`height:46%` では
フォントが `1.85vh` 側で効く画面比のとき **46.18%** 必要でした。0.18%の不足です。

実際に崩れていた解像度（実測）：**1600×900 / 1440×820 / 1366×768 / 1280×720**。
1400×868 だけがギリギリ通っていたので、気づきにくい状態でした。

`50%` に広げて全サイズ解消。文字は枠の上端から始まるので、
枠を広げても**見た目は変わりません**（1400×868 での右ページ差分は 0px）。

---

## 6. 段落間の数値の根拠

設計データを画素実測しました。段間は一律ではなく、意図的に差がついていました。

| | 設計実測（中心間） | v9 | v10.1 |
|---|---|---|---|
| 見出し群 ↔ 説明文 | 約15px | 8.8px | 約15px |
| 施設名 ↔ 間取り | 約30px（中心間54px） | 6.4px | 約26px |

---

## 7. できなかったこと2つ（ご判断をお願いします）

### ① 奥付のタイトルロゴ拡大 → v9 のままにしました

タイトルもマークも**どちらも枠の高さに対する％**なので、枠を広げても
両方が同じ比率で大きくなるだけで、余りは増えません（実測で確認しました）。
v9 の余りは、全画面サイズ中いちばん厳しいところで **5.2px しかありません**。
39% → 40% にするだけで 6px 必要なので、**タイトルだけを大きくすることは
物理的にできませんでした。**

マークを縮めれば可能です。STUDIO 側の `<style>` に1行足すだけで切り替わります。

```
.m{--fltH:47%;--fmkH:50%}   ← タイトル +21% / マーク -12%
.m{--fltH:44%;--fmkH:53%}   ← タイトル +13% / マーク  -7%
```

どちらにするか、あるいはマークは触らないか、ご指示ください。

### ② とまるの「見出し群 ↔ 説明文」 → v9 のままにしました

とまるだけ幅が足りず、ご指示にあった「施設名↔間取り」のあきだけ入れています。
ここを広げるには、ノド側の余白（現在133px／設計データは18px）を詰める必要があり、
**ブロック全体が右へ動きます。** 位置が変わるので勝手にはやりません。

設計データではとまるの情報ブロックは右端が 682px（ノドから18px）まで来ています。
設計に合わせるなら `.k1L .ibs{right:3%}` の1箇所です。

---

## 8. Safari 対策は入っていません

手元で Safari を動かせない以上、検証できない変更を混ぜるべきではないと判断しました。
v10.1 が問題なく動くことを確認いただいたあと、**単独で**着手させてください。

次に試す価値がある方式は「`will-change:transform` を常時9枚ではなく、
いまめくっている紙だけに付ける」です。座標も奥行きも一切変えないので、
v10.0 のような副作用は構造上起きません。CSS 1行の変更で、戻すのも1行です。

---

## 9. 実機で確認していただきたいこと

- **Safari / Firefox での表示**（コンテナから動かせません）
- **縦中横の見え方** … `10台` `30分` `21時` の数字が横に並んで見えるか。
  仕様上は Safari も対応していますが、実機での裏が取れていません
- **詰めコンマ** … `3,300円` `1,500円` のコンマの詰まり具合

---

## 10. 嘉左衛門の情報欄に文言を足すとき

`<br>` が「桁の変わり目」です。**1桁に入るのは17字まで**なので、
どの行も17字以内で切ってあります。超えると、その行だけ自動で折り返して
桁が1つ増え、左に伸びます。
