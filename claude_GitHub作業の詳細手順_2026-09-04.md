# GitHub作業の詳細手順 — `book.v2.js` の追加と `v2.0.0` タグの作成

作成日：2026年9月4日
対象リポジトリ：`tukinowa-git/tukimichi-test`
所要時間：15分（うち待ち時間3分）
※ この文書は `claude/body末尾移行_Phase0手順_2026-09-04.md` の **§4 を詳しく置き換えるもの**です。

---

## 0. まず全体の地図

やることは**2つだけ**です。

```
【作業1】book.v2.js というファイルをリポジトリに置く
              ↓
        GitHub Pages が自動で配信を始める（30秒〜2分待つ）
              ↓
        https://tukinowa-git.github.io/tukimichi-test/book.v2.js が開ける
              ↓
【作業2】v2.0.0 という「タグ（リリース）」を作る
              ↓
        jsDelivr が配信を始める
              ↓
        https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v2.0.0/book.v2.js が開ける
```

### なぜ2つ必要なのか

Studioに貼るコードには、同じファイルを**2つのURLで**書いてあります。

```html
<script defer src="https://tukinowa-git.github.io/tukimichi-test/book.v2.js"
  onerror="...cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v2.0.0/book.v2.js...">
```

- **前のURL（GitHub Pages）＝ 主系。**普段はこちらから読みます。**作業1だけで動きます**
- **後のURL（jsDelivr）＝ 副系。**GitHub Pages が落ちたときだけ使う保険。**作業2をしないと保険が効きません**

「作業1で動いたから作業2はいらない」と思いがちですが、そのとき副系は404のまま静かに壊れています。GitHub Pages が落ちる日は年に数回あります。**必ず両方やってください。**

タグ（リリース）は、リポジトリ全体をその時点で写真に撮って `v2.0.0` という名前を付けるようなものです。jsDelivr はこの写真を見に行きます。

---

## 1. 作業前の確認（1分）

ブラウザで開きます。

```
https://github.com/tukinowa-git/tukimichi-test
```

### 1-1. ログインしているか

画面右上に自分のアイコンが出ていればログイン済みです。「Sign in」と出ていたらログインしてください。**ログインしていないと、ファイルを追加するボタンが出ません。**

### 1-2. ファイル一覧を見る

いま次の4つがあるはずです。

| ファイル名 | 役割 |
|---|---|
| `.nojekyll` | 空ファイル。**絶対に消さない**（GitHub Pagesが余計な処理をしないためのおまじない） |
| `README.md` | 自分あてのメモ。動作には無関係 |
| `book.v1.css` | 見た目のルール集。**今回は触りません** |
| `book.v1.js` | 今動いているめくりエンジン。**消さずに残します** |

### 1-3. ブランチ名を確認する

ファイル一覧の左上に、枝分かれのアイコンと一緒に **`main`** と書かれたボタンがあります。ここが `main` か `master` かをメモしてください。この後のURLで使います。

普通は `main` です。以降は `main` として書きます。`master` だった場合は、出てくるURLの `main` を `master` に読み替えてください。

---

## 2. 【作業1】`book.v2.js` を追加する（5分）

やり方は2つあります。**方法A（アップロード）のほうが失敗しません。**

---

### 方法A：ファイルをアップロードする（推奨）

#### A-1. ダウンロードしたファイルの名前を確認する

チャットから `book.v2.js` をダウンロードしてください。ダウンロードフォルダで、ファイル名が

```
book.v2.js
```

**きっかりこの通り**になっているか確認します。もし次のようになっていたら、右クリック →「名前の変更」で直してください。

| よくある間違い | 直す |
|---|---|
| `book.v2` （拡張子なし） | → `book.v2.js` |
| `book.v2.js.txt` | → `book.v2.js` |
| `book.v2 (1).js` | → `book.v2.js` |
| `book_v2.js` （アンダースコア） | → `book.v2.js` |

**ピリオドとスペルが1文字違うだけで読み込めません。** ここが一番の落とし穴です。

> Windowsで拡張子が見えない場合：エクスプローラーの「表示」タブ →「ファイル名拡張子」にチェックを入れると見えるようになります。
> Macで拡張子が見えない場合：Finder → 設定 → 詳細 →「すべてのファイル名拡張子を表示」にチェック。

#### A-2. アップロード画面を開く

このURLを直接開きます。ボタンを探すより確実です。

```
https://github.com/tukinowa-git/tukimichi-test/upload/main
```

（メニューからたどる場合：リポジトリ画面の右上あたりにある緑ではない **「Add file ▾」**（日本語UIなら「ファイルを追加」）ボタン → **「Upload files」**（「ファイルをアップロード」））

#### A-3. ファイルを放り込む

画面の真ん中に点線の枠と「Drag files here to add them to your repository」（ここにファイルをドラッグ）と書かれた領域があります。

- ここに `book.v2.js` を**ドラッグ＆ドロップ**します
- または「choose your files」（ファイルを選択）のリンクをクリックして選びます

枠の下にファイル名が1行表示されれば取り込めています。**`book.v2.js` と表示されているか、ここでもう一度確認してください。**

#### A-4. コミットする

画面を下にスクロールします。

1. **Commit message**（コミットメッセージ）の欄に、既定で `Add files via upload` と入っています。そのままでも構いませんが、後で見て分かるように書き換えるのがおすすめです。

   ```
   book.v2.js を追加（body末尾カスタムコード対応）
   ```

2. その下のラジオボタンで **「Commit directly to the `main` branch」**（`main` ブランチに直接コミットする）が**選ばれていること**を確認します。

   > もう一方の「Create a new branch for this commit and start a pull request」を選ぶと、ファイルは別の場所に入ってしまい、GitHub Pages に反映されません。**必ず上を選んでください。**

3. 緑色の **「Commit changes」**（変更をコミット）ボタンを押します。

#### A-5. 追加されたことを確認する

リポジトリのファイル一覧に戻ります。**5つ**になっていれば成功です。

```
.nojekyll
README.md
book.v1.css
book.v1.js
book.v2.js   ← 増えた
```

`book.v2.js` をクリックすると中身が表示されます。1行目が次のようになっていれば正しく入っています。

```
/* book.v2.js — 本のめくりエンジン ＋ Instagram連携
```

---

### 方法B：中身を貼り付けて作る

ダウンロードがうまくいかないときはこちら。

#### B-1. 新規ファイル作成画面を開く

```
https://github.com/tukinowa-git/tukimichi-test/new/main
```

（メニューからたどる場合：**「Add file ▾」** → **「Create new file」**（新しいファイルを作成））

#### B-2. ファイル名を入力する

画面上部に、

```
tukimichi-test  /  [ Name your file... ]
```

という入力欄があります。ここに次を入力します。

```
book.v2.js
```

> **注意：`/`（スラッシュ）を入れないこと。** `js/book.v2.js` のように書くと `js` フォルダが作られ、URLが変わって読み込めなくなります。**ファイル名だけ**を入れてください。

#### B-3. 中身を貼り付ける

その下の大きな黒い（または白い）編集エリアをクリックし、`book.v2.js` の中身を**全部**貼り付けます。

貼り付け元での全選択は `Ctrl+A`（Macは `⌘+A`）、コピーは `Ctrl+C`（`⌘+C`）です。

貼り終わったら、**最後の行**が次のようになっているか確認します。

```
})();
```

途中で切れていたら貼り直しです。

#### B-4. コミットする

右上の緑色の **「Commit changes...」** ボタンを押すと、小さなウィンドウが開きます。

1. Commit message：`book.v2.js を追加（body末尾カスタムコード対応）`
2. **「Commit directly to the `main` branch」** が選ばれていることを確認
3. 緑色の **「Commit changes」** を押す

---

## 3. GitHub Pages への反映を待つ（1〜3分）

ファイルを置いても、配信が始まるまで少し時間がかかります。

### 3-1. 進み具合を見る

```
https://github.com/tukinowa-git/tukimichi-test/actions
```

（メニューからたどる場合：リポジトリ上部のタブ **「Actions」**）

一番上に **「pages build and deployment」** という行があります。

| 印 | 状態 |
|---|---|
| 🟠 オレンジの丸（くるくる回る） | 配信の準備中。待つ |
| ✅ 緑のチェック | **完了。次へ進める** |
| ❌ 赤いバツ | 失敗。§7 へ |

普通は30秒〜2分で緑になります。

> Actionsタブに何も出ない場合でも、GitHub Pages は動いていることがあります。3分待ってから §4 の確認に進んでください。

---

## 4. 主系URLが開けるか確認する（1分）

ブラウザの**新しいタブ**でこのURLを開きます。

```
https://tukinowa-git.github.io/tukimichi-test/book.v2.js
```

### 成功したときの画面

黒っぽい文字でプログラムが表示されます。先頭が次のようになっていればOKです。

```
/* book.v2.js — 本のめくりエンジン ＋ Instagram連携
   book.v1.js からの変更点（2026-09-04 / Studio body末尾カスタムコードへの移設に対応）
```

> ブラウザによっては表示せずにダウンロードが始まることがあります。それも正常です。ダウンロードされたファイルをメモ帳などで開いて中身を確かめてください。

### 「404 File not found」と出たとき

慌てなくて大丈夫です。順に確かめます。

1. **もう2分待ってリロード**（`Ctrl+Shift+R` / `⌘+Shift+R` で強制リロード）
2. それでもダメなら、URLのスペルを1文字ずつ照合する
   - `tukinowa-git`（ハイフンあり）
   - `tukimichi-test`（ハイフンあり）
   - `book.v2.js`（ピリオド2つ）
3. リポジトリのファイル一覧に戻り、ファイル名が `book.v2.js` になっているか見る
4. それでもダメなら §7 へ

---

## 5. 【作業2】`v2.0.0` タグ（リリース）を作る（5分）

**ここを飛ばすと、副系（jsDelivr）が404のまま静かに壊れます。**

### 5-1. リリース作成画面を開く

```
https://github.com/tukinowa-git/tukimichi-test/releases/new
```

（メニューからたどる場合：リポジトリ画面の**右側のサイドバー**に「Releases」という見出しがあります。まだ1つも無ければ「Create a new release」、既にあれば数字をクリック → 右上の「Draft a new release」）

### 5-2. タグ名を入力する ← ここが唯一の難所

画面の左上に **「Choose a tag ▾」**（タグを選択）というグレーのボタンがあります。

1. これを**クリック**すると、検索欄付きの小さなリストが開きます
2. 検索欄に次を**手で入力**します

   ```
   v2.0.0
   ```

3. 入力すると、リストの中に

   ```
   + Create new tag: v2.0.0 on publish
   ```

   （日本語UIなら「公開時に新しいタグ `v2.0.0` を作成」）という項目が現れます。**これをクリックします。**

> **入力しただけでは作られません。** 必ずこの「Create new tag ... on publish」の行をクリックしてください。クリックすると、ボタンの表示が「Choose a tag」から **「v2.0.0」** に変わります。これが押せた合図です。
>
> `v` は小文字。`V2.0.0` や `2.0.0` では、Studioに貼ったコードのURLと一致しません。

### 5-3. 対象ブランチを確認する

タグ名の右隣に **「Target: main ▾」** と表示されています。`main` になっていればそのままでOKです。

### 5-4. タイトルを入れる

**「Release title」**（リリースタイトル）の欄に入力します。中身は自由ですが、後から見て分かるものを。

```
v2.0.0 — book.v2.js 追加（body末尾カスタムコード対応）
```

説明欄（Describe this release）は空でも構いません。書くなら：

```
book.v1.js からの変更点
- ホイール・タッチ・キーの反応を本の内側に限定
- 起動リトライに上限を追加
- めくり影(.fs)の重複生成を修正
- Instagram連携に起動条件を追加
```

### 5-5. 公開する

画面下部のチェックボックスは既定のままで構いません（「Set as the latest release」にチェックが入っていればOK）。

緑色の **「Publish release」**（リリースを公開）ボタンを押します。

> 隣に「Save draft」（下書きを保存）というボタンがありますが、**これでは公開されません。**必ず「Publish release」のほうを押してください。

### 5-6. できたことを確認する

```
https://github.com/tukinowa-git/tukimichi-test/releases
```

`v2.0.0` が一覧に出ていれば成功です。既存の `v1.0.0` もそのまま残っているはずです。**v1.0.0 は消さないでください**（後戻りの保険です）。

---

## 6. 4つのURLをまとめて確認する（2分）

Studioに進む前の**最終チェック**です。4つとも中身が表示されることを確認してください。

| # | URL | 期待する中身 |
|---|---|---|
| 1 | `https://tukinowa-git.github.io/tukimichi-test/book.v2.js` | `/* book.v2.js — 本のめくりエンジン` で始まる |
| 2 | `https://tukinowa-git.github.io/tukimichi-test/book.v1.css` | `/*! book.v1.css —` で始まる |
| 3 | `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v2.0.0/book.v2.js` | 1と同じ中身 |
| 4 | `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v2.0.0/book.v1.css` | 2と同じ中身 |

> **3と4が「Couldn't find the requested file」と出たとき：** jsDelivr は初回アクセス時にGitHubから取りに行くので、タグを作った直後は1〜2分かかることがあります。少し待ってリロードしてください。それでもダメならタグ名（`v2.0.0`）のスペルを確認します。

**4つとも通ってから Studio へ進んでください。** ここで妥協すると、Studio側で本が動かなかったときに「移設の失敗」なのか「ファイルが届いていないだけ」なのか判別できなくなります。

---

## 7. うまくいかないときの対処

| 症状 | 原因 | 直し方 |
|---|---|---|
| ファイル追加のボタンが無い | ログインしていない／自分のリポジトリではない | 右上からログイン |
| `404 File not found`（主系） | 反映待ち／ファイル名の綴り違い／フォルダの中に入ってしまった | 3分待つ → リポジトリでファイル名と場所を確認 |
| ファイルが `js/book.v2.js` になっている | ファイル名に `/` を入れた | §8-1 で削除して作り直す |
| Actionsが赤いバツ | `.nojekyll` が消えている可能性 | ファイル一覧に `.nojekyll` があるか確認。無ければ空ファイルとして作り直す |
| `Create new tag ... on publish` が出ない | 入力欄ではなく別の場所に打っている | 「Choose a tag」を一度クリックし、開いた検索欄に入力する |
| タグは作ったのにjsDelivrが404 | 反映待ち／タグ名の綴り違い | 2分待つ → `/releases` でタグ名が `v2.0.0` か確認 |
| 中身が古いまま表示される | ブラウザのキャッシュ | 強制リロード（`Ctrl+Shift+R` / `⌘+Shift+R`）またはシークレットウィンドウで開く |
| ファイルの中身が途中で切れている | 貼り付けミス | §8-2 で編集して貼り直す |

---

## 8. 間違えたときの直し方

### 8-1. ファイルを削除する

1. リポジトリでそのファイル名をクリック
2. 右上の **「…」** メニュー → **「Delete file」**（ファイルを削除）
3. 下までスクロールして緑の **「Commit changes」**

### 8-2. ファイルの中身を書き換える

1. リポジトリでそのファイル名をクリック
2. 右上の**鉛筆アイコン**（Edit this file）をクリック
3. `Ctrl+A` で全選択して削除 → 正しい中身を貼り直す
4. 緑の **「Commit changes...」**

### 8-3. タグ（リリース）を作り直す

1. `https://github.com/tukinowa-git/tukimichi-test/releases`
2. 間違えたリリースをクリック → 右上の**ゴミ箱アイコン** → 「Delete」
3. **これだけではタグは消えません。** タグも消すには、リポジトリ画面のブランチ選択ボタン →「Tags」タブ → 該当タグの右の「…」→「Delete tag」
4. §5 からやり直す

> タグを消して同じ名前で作り直した場合、jsDelivr が古い内容を覚えていることがあります。その場合は `v2.0.1` のように別の名前で作り、Studio側のコードの `@v2.0.0` も合わせて書き換えるのが確実です。

---

## 9. 作業完了後のリポジトリの姿

```
tukinowa-git/tukimichi-test
├── .nojekyll        （空ファイル・消さない）
├── README.md
├── book.v1.css      （現役・今回は変更なし）
├── book.v1.js       （旧版・後戻り用に残す）
└── book.v2.js       ← 今回追加

Releases
├── v1.0.0           （残す）
└── v2.0.0           ← 今回追加
```

**古いものを何ひとつ消していない**のがこの構成の要点です。Studioに貼ったコードの `book.v2.js` を `book.v1.js` に、`@v2.0.0` を `@v1.0.0` に書き戻すだけで、いつでも1分で元に戻せます。

---

## 10. 次の作業へ

ここまで終わったら、`claude/body末尾移行_Phase0手順_2026-09-04.md` の **§5「Studio側の作業」** へ進んでください。

Studio側の要点を先に3つだけ：

1. トップページを**複製**してから作業する（本番には触らない）
2. 貼る先は**ページ設定**のカスタムコード → `</body>` 直前（サイト設定ではない）
3. 複製ページの**Embedボックスは削除**する

---

## 付録：GitHubのボタン対訳

GitHubは設定によって英語表示と日本語表示が切り替わります。

| 英語 | 日本語 | 場所 |
|---|---|---|
| Add file | ファイルを追加 | リポジトリ上部 |
| Create new file | 新しいファイルを作成 | Add fileのメニュー内 |
| Upload files | ファイルをアップロード | Add fileのメニュー内 |
| Commit changes | 変更をコミット | 保存ボタン（緑） |
| Commit directly to the main branch | mainブランチに直接コミットする | 保存前のラジオボタン |
| Releases | リリース | リポジトリ右サイドバー |
| Draft a new release | 新しいリリースを下書き | リリース一覧の右上 |
| Choose a tag | タグを選択 | リリース作成画面の左上 |
| Create new tag: v2.0.0 on publish | 公開時に新しいタグ v2.0.0 を作成 | タグ入力後に現れる選択肢 |
| Publish release | リリースを公開 | リリース作成画面の下部（緑） |
| Edit this file | このファイルを編集 | ファイル表示中の鉛筆アイコン |
| Delete file | ファイルを削除 | ファイル表示中の「…」メニュー |
