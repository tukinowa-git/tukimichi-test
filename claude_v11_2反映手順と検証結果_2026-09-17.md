# 月みちて、山となる。 v11.2 反映手順と検証結果（2026-09-17）

v11.1.0 の確認後にいただいた修正指示（3回目）を反映した版です。
今回は画像の差し替えがあるため、画像の更新手順も含めています。

## 1. 納品物

| ファイル | 置き場所 | 内容 |
|---|---|---|
| `img_v11_2.zip`（中身は下の8枚） | GitHub の `img/` フォルダ | 差し替え用の写真（WebP） |
| `book.v11.2.css` | GitHub リポジトリ直下 | スタイル本体 |
| `book.v11.2.js` | GitHub リポジトリ直下 | エンジン本体（めくり対策の2行を追加） |
| `claude_PC版embedコード_v11_2_2026-09-17.html` | STUDIO トップのカスタムコード | 27,633字（残り2,367字） |
| `claude_嘉左衛門ページembedコード_v11_2_2026-09-17.html` | STUDIO /stay/kazaemon のカスタムコード | 版の表記のみ変更 |

### 差し替え用の写真（すべて既存と同じファイル名です）

| ファイル名 | 元の画像 | 大きさ | 容量 |
|---|---|---|---|
| p05-shiru-xs.webp | p05-shiru-main.jpg | 1650×1100 | 283KB |
| p05-shiru-sub1.webp | p05-shiru-sub1.JPG | 1000×667 | 47KB |
| p05-shiru-sub2.webp | p05-shiru-sub2.JPG | 1000×667 | 62KB |
| p06-tamesu-xs.webp | p06-tamesu-main.JPG | 1650×1100 | 134KB |
| p06-tamesu-sub1.webp | p06-tamesu-sub1.JPG | 1000×667 | 79KB |
| p08-sumau-xs.webp | p08-sumau-main.JPG | 1650×1100 | 170KB |
| p08-sumau-sub1.webp | p08-sumau-sub1.jpg | 1000×667 | 42KB |
| p08-sumau-sub2.webp | p08-sumau-sub2.jpg | 1000×667 | 123KB |

- 見開きの大きな写真（xs）は幅1650px、片ページの写真（sub）は幅1000pxにしました。いずれも画面表示の約2倍で、Retina 画面でも粗くなりません。
- 元の写真（1枚2〜19MB）の約1〜2%の容量です。
- **元の写真の多くには、撮影場所の位置情報（GPS）が入っていました。変換した WebP では、位置情報を含む撮影情報をすべて取り除いてあります。**
- ファイル名の「main」は、サイト上の「xs」（見開きをまたぐ写真）に当たります。

## 2. 反映手順（この順番を守ってください）

### 手順1　写真を差し替える（GitHub）

1. `img_v11_2.zip` を展開します（8枚の .webp が出てきます）。
2. ブラウザで `https://github.com/tukinowa-git/tukimichi-test` を開き、`img` フォルダをクリックします。
3. 右上の「Add file」→「Upload files」を選びます。
4. 8枚をまとめてドラッグ＆ドロップします。同じ名前のファイルは上書きされます。
5. 下の「Commit changes」の欄に、「v11.2 写真差し替え（しる・ためす・すまう）」などと入れ、「Commit changes」を押します。

### 手順2　CSS・JS を置く

1. リポジトリの一番上（`tukimichi-test`）に戻ります。
2. 「Add file」→「Upload files」で、`book.v11.2.css` と `book.v11.2.js` をアップロードし、「Commit changes」を押します。

### 手順3　タグを切る

1. リポジトリ右側の「Releases」→「Draft a new release」（または「Create a new release」）を開きます。
2. 「Choose a tag」に `v11.2.0` と入力し、「Create new tag」を選びます。
3. タイトルにも `v11.2.0` と入れ、「Publish release」を押します。

### 手順4　開けることを確認する

次のURLをブラウザで開き、中身や新しい写真が表示されれば準備完了です。

- `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.2.0/book.v11.2.css`
- `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.2.0/book.v11.2.js`
- `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.2.0/img/p05-shiru-xs.webp`（森の池の写真）
- `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.2.0/img/p08-sumau-xs.webp`（5人の写真）

タグを切った直後は、表示まで1〜2分かかることがあります。

### 手順5　STUDIO に貼る

トップと /stay/kazaemon の両方に、新しい埋め込みコードを貼って公開します。

### 補足

- 画像のURLにタグ（`@v11.2.0`）が入っているので、公開後に古い写真がキャッシュで残ることはありません。
- 手順3を飛ばして手順5を行うと、タグが存在しないため何も表示されません。
- 元に戻すときは、v11.1 の埋め込みコード2本を貼り直すだけです（v11.1.0 のタグは古い写真のまま残っています）。

## 3. めくり中のチラつき・カクつきについて

### 原因

v11.1 で、お問い合わせの地図（Google マップ2枚、白黒フィルター付き）を、右ページから左ページへ移しました。この左ページは「本の一番下に敷いてある土台」で、ほかの見開きをめくっている間も、紙の下で描画され続けていました。

さらに、地図には「ページを開いて12秒後に必ず読み込む」保険の処理があります。そのため、しばらく見ていると、**どの見開きのめくりでも、重い地図2枚が毎コマ合成に加わる**状態になっていました。

v11.0 までは、地図は「裏返って非表示になっている紙の面」にあったため、描画されていませんでした。v11.1 から目立つようになったのはこのためです。

Chrome の開発者ツールの機能で、めくり中に合成されている層を比べました。

| 版・状況 | 地図の層 | 層の数 |
|---|---|---|
| v11.1　とまる→たべる をめくる間 | あり | 37〜39 |
| v11.2　同じ操作 | なし | 33〜35（v11.0 と同じ水準） |
| v11.2　すまう→お問い合わせ をめくる間 | あり（必要なため） | — |

### 対策

お問い合わせの左ページは、次のときだけ描画するようにしました（CSS 1行＋JS 2行）。

- 最後の見開きを開いているとき
- 最後の紙（すまう⇔お問い合わせ）をめくっている間

それ以外のときは紙の下に隠れていて見えないので、見た目は変わりません。
地図の操作（拡大・移動）と、地図の上ではめくれない動きも、そのままです。

### もう一つの効果

今回の写真差し替えでは、画面に合った大きさの WebP にしています。めくりの途中で写真を展開する負荷も小さく抑えています。

## 4. 修正内容

### 05 しる

**写真**
3枚とも差し替えました。仮置きのグレー表示と文字は外しています。
メイン写真は、見開きの境（ノド）が人物の間に来ることを確認しています。

**サブ写真2枚**
すまうのサブ写真と同じ縦横比（1400×868 で 1.242）の横長にしました。どの画面サイズでも、すまうと同じ比率になります。
天地は、前回のご指示どおりメイン写真とそろえたままです。そのため、写真はナビ側（左）へ広がっています（1400×868 で約293×236px）。

**2つのブロックの天地**
体験ツアー・ガイドとワークショップの天と地を、右ページの本文「「西川町の魅力はなに？」と…」の枠と一致させました。8つの画面サイズすべてで差は0pxです（1400×868 で 592〜811px）。

本文の枠は1列14字分の高さのため、この見開きの説明文だけ字を少し小さくしています（13.0→12.6px）。こうしないと、体験ツアー・ガイドの説明が2列に収まりません（1列17字が必要）。

### 06 ためす

メイン写真とサブ写真1を差し替えました。
サブ写真1は横長の枠で上下が切れるため、頭が切れないよう、写真の上寄りを表示しています。

### 07 つくる

「creative studio」の字面の右端を、上の写真の右端にそろえました。8サイズすべてで差は±1.2px以内です。

左へ寄せたぶん、左端のブロック（FABEL 月山）がナビに近づくため、2つのブロックの間を少し詰めました（1400×868 で 24→14px）。ナビとの間は最小13pxです。

### 08 すまう

写真3枚を差し替えました。
メイン写真は、ノドが人物にかからないよう、写真の左側を基準に表示しています。

### 09 お問い合わせ

**左ページ**

- **タイトルロゴ**：約2.8倍にしました（字面の高さ 1400×868 で 90→252px）。
  3倍にすると、ノートPCでよくある画面（1440×789など）で、住所と助成の文の間が5px前後しか残りません。そのため2.8倍にしています。
  埋め込みコードの `<style>` に `.m{--fltH:45vh}` と書けば3倍にできます（ただし上記のとおり窮屈になります）。
- **ロゴマーク**：大きさは変えていません。
- **ツキノワのロゴの枠**：「運営　ツキノワ合同会社」と住所の間に確保しました。高さ 4.6vh（1400×868 で約40px）、仮置きの枠は横120pxで、点線と「ツキノワのロゴ」の文字を表示しています。

**右ページ（フォーム）**

| 項目 | v11.1 | v11.2 |
|---|---|---|
| 1行目の位置 | 82px | 130px |
| 入力項目の間隔 | 14px | 26px |
| 内容欄の高さ | 126px | 190px |
| 送信ボタンの下の余白 | 317px | 144px |

数値はすべて1400×868のときです。1440×789 では、内容欄は 71→145px です。

**ツキノワのロゴが届いたら**

1. ロゴを `img/` にアップロードします。高さ120px以上の PNG・WebP（背景透過）か SVG を推奨します。名前は例えば `logo-tsukinowa.webp` にします。
2. トップの埋め込みコードの次の1行を探します。
   `<div class="ftl tmpb" data-lbl="ツキノワのロゴ" aria-hidden="true"></div>`
3. その1行を、次の1行に置き換えます。
   `<img class="ftl" data-i="logo-tsukinowa.webp" alt="ツキノワ合同会社">`
4. 新しいタグを切り、埋め込みコードの版を書き換えて公開します（手順1〜5と同じ流れです）。

ロゴの高さは `.m{--ftlH:4.6vh}` で変えられます。

## 5. 検証結果

ローカルに STUDIO と同じ条件（Typekit 適用・jsDelivr の差し替え）の描画環境を作り、Chrome で確認しました。
画面サイズは、1400×868／1512×870／1440×789／1366×768／1280×720／1920×1080／1440×900／1100×700 の8つです。

- **しる**：2つのブロックの天地と本文の枠、上下の写真とメイン写真の天地が、8サイズすべてで一致しました。17字の組み方も崩れていません。
- **つくる**：字面の右端と写真の右端の差は、8サイズすべてで±1.2px以内です。
- **お問い合わせ**：左ページの各要素は重なっていません。住所と助成の文の間は最小16pxです。内容欄は最小132px（1280×720）です。
- **写真**：差し替えた8枚がすべて読み込まれることを確認しました。
- **めくり**：ホイール・左ナビとも正常です。地図の上ではめくれません。お問い合わせから他の見開きへ移ると、左ページは再び描画されなくなります。
- **リンク**：全見開きの全リンク・ボタンで、クリックできないものは0件です。
- **動いていないことの確認**：1400×868 と 1512×870 で v11.1 と比べました。動いたのは、ご指示のあった箇所（しるの写真・ブロック、つくるのブロック、お問い合わせ）だけです。嘉左衛門ページは、見た目の変化はありません。

## 6. 補足

- **検証環境の限界**：この検証環境は画面描画を CPU で行うため、実機のチラつきそのものは再現できません。原因の層が、めくり中に合成されなくなったことを確認した、という結果です。反映後もチラつきが残る場合は、ブラウザの種類（Safari / Chrome）と、どの見開きで起きるかを教えてください。
- **目次の説明**：目次のしるの説明は「ツアーガイドとワークショップ」のままです（持ち越し）。
- **v11.1 のファイル名の日付**：「2026-09-18」は誤りで、正しくは 2026-09-17 です。中身には影響ありません。
