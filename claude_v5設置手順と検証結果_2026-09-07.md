# v5 設置手順と検証結果

2026-09-07 ／ 月みちて、山となる。 PC版トップ
嘉左衛門詳細ページ対応の第1段階（トップ側と共通部分）

---

## 1. この段階で何が変わるか

**見た目は変わりません。** 変わるのは次の3点だけです。

1. とまる見開きの「詳細を見る」が、嘉左衛門だけ実際に `/stay/kazaemon` へ飛ぶようになる
2. 平左衛門の「詳細を見る」が一時的に消える（ページ準備中のため）
3. `#s3` のようなURLで来たとき、ローディング演出を飛ばしてその見開きから始まる

3番目は嘉左衛門ページを作ってから効いてきます。今は仕込みだけです。

---

## 2. ファイル

| ファイル | 文字数 | 置き場所 |
|---|---:|---|
| `book.v5.css` | 27,060 | GitHub `tukinowa-git/tukimichi-test` |
| `book.v5.js` | 15,894 | 同上 |
| `claude_PC版embedコード_v5_2026-09-07.html` | 25,887 | STUDIO のカスタムコード欄（上限30,000／残り4,113） |

**v1〜v4は消さないでください。** 戻すときに要ります。

---

## 3. 設置手順

### 手順1：GitHubにアップロード

`book.v5.css` と `book.v5.js` をリポジトリの直下に追加します。既存ファイルは触りません。

### 手順2：リリースタグ `v5.0.0` を作成

Releases → Draft a new release → タグ名に `v5.0.0` と入力して Publish。

このタグは jsDelivr の副系（GitHub Pagesが落ちたときの予備）が参照します。**タグを作り忘れると、GitHub Pagesが落ちた瞬間に本が出なくなります。**

### 手順3：GitHub Pages に反映されたか確認

ブラウザで次の2つを開き、中身が表示されることを確認します。反映まで数分かかることがあります。

```
https://tukinowa-git.github.io/tukimichi-test/book.v5.css
https://tukinowa-git.github.io/tukimichi-test/book.v5.js
```

### 手順4：STUDIOに貼る

STUDIO → トップページを選択 → 右パネルの［ページ］タブ → カスタムコード →
`</body>` 直前の欄を**全選択して削除し**、`claude_PC版embedコード_v5_2026-09-07.html` の中身を全文貼り付け。

**SP用Embedボックスは絶対に削除しないでください。** スマホ表示はそちらが担当しています。

### 手順5：公開して確認

---

## 4. 公開後に見ていただきたいこと

- [ ] 全10見開きが従来どおり表示される
- [ ] ホイール・キー・クリックでのめくりが従来どおり
- [ ] 左ナビ、右下の2ボタン、目次からのジャンプが従来どおり
- [ ] とまる見開きの「詳細を見る」が**嘉左衛門側だけ**になっている
- [ ] 嘉左衛門の「詳細を見る」を押すと、**中身だけがすっと消えてから**404ページへ飛ぶ
      （`/stay/kazaemon` はまだ無いので404で正常です）
- [ ] お問い合わせフォームの送信が従来どおり
- [ ] ブラウザ幅を768px以下にするとPC版が消え、SP版が出る

「中身だけがすっと消えてから」の部分が、嘉左衛門ページへの遷移演出の前半です。
背景・左ナビ・右下ボタン・写真の枠は動かず、紙の上の文章と写真だけが沈んで消えます。

---

## 5. 元に戻す方法

STUDIOのカスタムコード欄で置換するだけです。GitHubは触りません。

| 置換前 | 置換後 |
|---|---|
| `book.v5.css` | `book.v4.css` |
| `book.v5.js` | `book.v4.js` |
| `@v5.0.0` | `@v4.0.0` |

これで完全にv4の状態に戻ります。

---

## 6. 検証の記録

Playwright + Chromium 141 で、v4 と v5 を同条件（1400×868／地図タイルとInstagram実データは両版とも遮断／画像とCSS背景の読み込み完了を待機）でレンダリングし、全10見開きを画素単位で比較しました。

| 見開き | 差分画素 | 判定 |
|---|---:|---|
| 00 ローディング | 0 | 完全一致 |
| 01 イントロ／目次 | 0 | 完全一致 |
| 02 日々のあれこれ | 0 | 完全一致 |
| 03 とまる | 357 | 平左衛門の「詳細を見る」を消した箇所のみ |
| 04 たべる | 0 | 完全一致 |
| 05 しる | 0 | 完全一致 |
| 06 ためす | 0 | 完全一致 |
| 07 つくる | 0 | 完全一致 |
| 08 すまう | 0 | 完全一致 |
| 09 お問い合わせ | 0 | 完全一致 |

**合計 357画素／全12,152,000画素。** 差分は x107–117 / y728–812 の1箇所に限られ、目視でも平左衛門の「詳細を見る」とその罫線が消えただけでした。JSエラーはゼロ。

CSSは第1〜10章がv4と一字一句同一であることをプログラムで照合済みです。追加したのは第11章のみで、そのセレクタはすべて `.lv` / `.ar` / `.dl` が付いているときだけ効きます。

### 新しい挙動の確認

**到着の待ち受け**（エンジンが届かない状況を作って観察）

```
待受クラス      : m ld pre dl ar dlnav dlcn
本の可視性      : hidden          ← 紙は伏せてある
左ナビ         : 定位置・不透明度1   ← 遷移元と同じ位置に出ている
右下ボタン      : 不透明度1
「とまる」の点灯  : 点灯している
背景          : 画像に差し替わっている（紙色の点滅なし）
5秒後         : m ld pre         ← 保険で待受が解ける
```

**離脱** — リンク先 `/stay/kazaemon`、`data-leave` は1個のみ、実クリックで0.43秒後に遷移。額縁（左ナビ）の不透明度は1のまま動かず。

**戻るボタン** — bfcache復元を模擬。中身が消えた状態が解除され、1.5秒後に通常状態へ復帰。「戻ると真っ白」は起きません。

### 検証中に見つけて直した点

遷移先が応答しない場合（通信断など）、中身が消えた空の本のまま取り残される穴がありました。`leave()` に6秒の自動復帰を追加しています。

---

## 7. v5 で追加したもの（技術メモ）

### book.v5.css 第11章

```
.lv   別ページへ出ていく途中（中身が沈みながら消える）
.ar   別ページから着いた直後（中身が浮かび上がる）
.dl   #s3 などで直接その見開きに来たときの待ち受け
--lvD 出ていく速さ（既定 .38s）／--arD 着く速さ（既定 .72s）
```

動かすのは「紙の上の中身」だけで、背景・左ナビ・右下ボタン・ノドの線・写真の枠は一切動かしません。出発ページと到着ページで額縁が同じ位置にあるため、途中を単色で覆わなくても継ぎ目が見えない、という設計です。

`prefers-reduced-motion: reduce` の利用者には演出を出しません。

### book.v5.js

v4の安全装置①〜⑧はすべてそのまま残しています。追加は3つ。

- **⑨ ディープリンク起動** — `#s1`〜`#s9` で来たら、ローディングを飛ばしてその見開きから始める。`?p=3` ではなく `#` を使うのは、`?` 付きURLが検索エンジンに別ページとして拾われるのを避けるためです（正規化の手当てが不要になります）。
- **⑩ 離脱** — `data-leave` 付きリンクは中身を送り出してから遷移。⌘/Ctrl/Shift/中クリックと `target` 付きは横取りしません（新しいタブで開く操作が壊れるため）。マウスが乗った時点で遷移先を先読みします。
- **⑪ 戻るボタン対策** — bfcacheから復元されたとき、⑩で消した中身を静かに戻します。

### 埋め込みコード

`</body>` 直前の先頭に、`#s1`〜`#s9` で来たときの待ち受け処理を追加しました。外部CSSやJSの到着を待たずに背景を差し替えるため、紙色の点滅が起きません。JSやGSAPが届かなかった場合は5秒で待ち受けが解けます。

---

## 8. 次の段階

ここまで問題なければ、嘉左衛門ページ本体（`/stay/kazaemon`）に進みます。

- `stay.v5.css` / `stay.v5.js`（詳細ページ専用。`book.v5.css` に追加ロード）
- 嘉左衛門ページの埋め込みコード
- STUDIOでのページ作成手順（`/stay` の親ページも同時に必要）
- リリースタグ `v5.1.0`

**トップ側のファイルはもう触りません。** 詳細ページ用は別ファイルとして追加するので、この段階の検証結果はそのまま有効です。
