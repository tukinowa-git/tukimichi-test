# PC版 v3 への差し替え手順

作成日：2026年9月5日／対象：月みちて、山となる。 PC版トップ
前提：CSS・JSはGitHub（`tukinowa-git/tukimichi-test`）、HTMLはSTUDIOの**ページ設定**→カスタムコード→`<body>`終了タグ直前

## いまのリポジトリの状態（2026年9月5日 実機確認済み）

```
tukinowa-git/tukimichi-test
├── .nojekyll
├── README.md
├── book.v1.css   （現役。今回 v3 へ）
├── book.v1.js    （旧版・後戻り用）
└── book.v2.js    （現役。9/4に追加。今回 v3 へ）

Releases: v1.0.0 / v2.0.0
```

CSSはこれまで一度も編集していないため、ファイル名の番号だけで言えば `book.v2.css` が筋です。ただ**タグが v3.0.0 になるのに CSS だけ v2 だと、貼り間違いのもと**です。今回はCSS・JS・タグを**すべて v3 に揃えました**。以後もこの方針で揃えるのが安全です。

---

## 0. 何が変わるか（3行）

1. **見開きが6→10に増えます。** ローディング／目次／Instagram／とまる／たべる／しる／ためす／つくる／すまう／お問い合わせ。とまる以降は1テーマ1見開きです。
2. **紙が6枚→9枚になるので、JSのページ配列とナビの行き先番号を全部振り直しています。** 古い `book.v1.js` のままだと動きません。**必ず3ファイル同時に差し替えてください。**
3. **差し替え待ちの写真18か所は、グレー＋ラベル表示**にしてあります。画面を見れば「たべる メイン」のようにどこに何を入れるか分かります。

---

## 1. 差し替えるファイル

| ファイル | 置き場所 | やること |
|---|---|---|
| `book.v3.css`（22,367字） | GitHub `tukimichi-test` リポジトリ直下 | **新規アップロード**（v1は消さずに残す） |
| `book.v3.js`（12,720字） | 同上 | **新規アップロード**（v1・v2は消さずに残す） |
| リリースタグ `v3.0.0` | GitHub → Releases | **新規作成**（jsDelivrの副系用） |
| `claude_PC版embedコード_v3_2026-09-05.html`（21,938字） | STUDIO →**ページ設定**→ カスタムコード →〈body〉終了タグ直前 | **中身を全部置き換え** |

> 古いものを何ひとつ消さないのがこの構成の要点です。うまく動かなければ、埋め込みコードの `v3` を `v2` / `v1` に書き戻すだけで元に戻せます。1ブロック30,000字の上限に対し21,938字なので余裕があります（残り8,062字）。

**SP用Embedボックスは絶対に削除しないでください。** スマホはSP版が担当します。

---

## 2. 手順

### ステップ1：GitHubに2ファイルを追加

1. `https://github.com/tukinowa-git/tukimichi-test` を開く
2. 「Add file」→「Upload files」
3. `book.v3.css` と `book.v3.js` をドラッグ＆ドロップ（ファイル名が1文字でも違うと読み込めません）
4. 「Commit directly to the `main` branch」を選んで「Commit changes」

### ステップ1-2：リリースタグ `v3.0.0` を作る

`https://github.com/tukinowa-git/tukimichi-test/releases/new` を開き、「Choose a tag」に `v3.0.0` と入力 →「**Create new tag: v3.0.0 on publish**」の行をクリック →「Publish release」。

この行をクリックするまでタグは作られません。ボタンの表示が `v3.0.0` に変われば成功です。

**確認**：下の4つをブラウザで直接開いて、コードが文字として表示されればOKです。

```
https://tukinowa-git.github.io/tukimichi-test/book.v3.css
https://tukinowa-git.github.io/tukimichi-test/book.v3.js
https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v3.0.0/book.v3.css
https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v3.0.0/book.v3.js
```

> GitHub Pagesは最大3分、jsDelivrはタグ作成後1〜2分かかります。404が出たら強制リロード（`Ctrl+Shift+R` / `⌘+Shift+R`）を挟んで待ってください。

### ステップ2：STUDIOの埋め込みコードを入れ替え

1. STUDIO →**該当ページの**ページ設定 → カスタムコード →〈body〉終了タグ直前のブロックを開く
2. 中身を**全選択して削除**
3. `claude_PC版embedコード_v3_2026-09-05.html` の中身を**全部**貼り付け
4. 保存 →「公開」

### ステップ3：確認

パスワードを入れてサイトを開き、以下を順に確認します。

- [ ] ローディングのロゴが左右から現れ、約5秒後に自動でめくれる
- [ ] 目次の6項目に説明文とカテゴリが2列で入っている
- [ ] Instagramが「右に最新1件・左に次の6件」で並ぶ
- [ ] とまる以降、ホイールで1見開きずつ進む（全部で10見開き）
- [ ] 左ナビの6項目を押すと、その見開きまでパラパラとめくれる
- [ ] 右下のメールボタンでお問い合わせへ、家ボタンで目次へ戻る
- [ ] とまる・しる・ためす・すまうの大きな写真が、ノド（背）をまたいで左右につながって見える
- [ ] お問い合わせの入力欄でホイールを回してもページがめくれない
- [ ] **ブラウザ幅を768px以下に縮めるとPC版の本が消え、SP版が出る**（ここが一番大事）

---

## 3. 差し替え待ちの写真 18か所

グレー表示になっているものが差し替え対象です。**埋め込みコードの中で `data-lbl="ラベル名"` を検索**すると、その画像のタグが見つかります。

| 見開き | ラベル | 位置 | 現在の仮画像 |
|---|---|---|---|
| 04 たべる | たべる メイン | 右ページ下 | 既存のとまる素材 |
| 04 たべる | カフェ店内 | 左ページ上 | 嘉左ヱ門素材 |
| 04 たべる | スイーツ | 左ページ下 | 平左ヱ門素材 |
| 05 しる | ツアーガイド | ノドをまたぐ大判（左右2か所） | 既存のとまる素材 |
| 05 しる | パンづくりWS | 左ページ上 | 嘉左ヱ門素材 |
| 05 しる | ワークショップ | 左ページ下 | 平左ヱ門素材 |
| 06 ためす | shino さん | ノドをまたぐ大判（左右2か所） | 既存のとまる素材 |
| 06 ためす | チャレンジキッチン | 左ページ上 | 嘉左ヱ門素材 |
| 06 ためす | シェアアトリエ | 左ページ下 | 平左ヱ門素材 |
| 07 つくる | つくる メイン | 右ページ | 既存のとまる素材 |
| 07 つくる | 縫製の手仕事 | 左ページ上 | 嘉左ヱ門素材 |
| 07 つくる | テキスタイル | 左ページ下 | 平左ヱ門素材 |
| 08 すまう | 移住者写真 | ノドをまたぐ大判（左右2か所） | 既存のとまる素材 |
| 08 すまう | インターン | 左ページ左下 | 嘉左ヱ門素材 |
| 08 すまう | 子どもが遊ぶ写真 | 左ページ右下 | 平左ヱ門素材 |
| 09 お問い合わせ | （マップ2枚） | 左ページ中央 | 嘉左ヱ門・平左ヱ門素材 |
| 09 お問い合わせ | 助成ロゴ | 左ページ左下 | 枠だけの仮表示 |

### 差し替えのやり方

1. STUDIOに画像をアップロードして、そのURLを控える
2. URLの `.../projects/xPORNDYyOr/` **より後ろの部分だけ**をコピー
3. 埋め込みコードの `data-i="ここ"` を書き換える
4. **`class="ph tmp"` の `tmp` を消す**（これでグレーとラベルが外れます）

```
差し替え前： <div class="ph tmp" data-lbl="カフェ店内" style="..."><img data-i="s-5760x3840_...jpg" alt=""></div>
差し替え後： <div class="ph"       　　　　　　　　　　 style="..."><img data-i="s-2000x1333_あたらしいID.jpg" alt="カフェ店内"></div>
```

ノドをまたぐ大判は `class="xs tmp"`（左ページ）と `class="xs r tmp"`（右ページ）の**2か所に同じ画像**を入れてください。片方だけだと真ん中で切れます。

---

## 4. 位置とサイズの調整

写真の位置は、タグの `style` に書いた数字だけで決まります。CSSを触る必要はありません。

### 片ページに収まる写真（`class="ph"`）

```
style="--l:9;--t:7.7;--w:82.2;--h:27.4"
```

| 記号 | 意味 |
|---|---|
| `--l` | 左からの位置（そのページの幅に対する%） |
| `--t` | 上からの位置（高さに対する%） |
| `--w` | 幅（%） |
| `--h` | 高さ（%） |

### ノドをまたぐ写真（`class="xs"`）

```
style="--x1:37.23;--x2:85.37;--y1:7.72;--y2:63.13"
```

こちらは**見開き全体**に対する%です。`0`＝見開きの左端、`50`＝ノド（背）、`100`＝右端。左ページ用と右ページ用に**同じ数字**を書けば、自動でつながります。

### 文字まわりのつまみ（埋め込みコード上部の `<style>`）

| 変えたいもの | 変数 |
|---|---|
| 左ナビの幅 | `--navW` |
| ローディングのロゴ幅・縦位置・縦横比 | `--logoW` `--logoY` `--logoAR` |
| タイトルロゴの幅・位置 | `--titleW` `--titleX` `--titleY` |
| コピー文の文字サイズ・位置 | `--introFS` `--introX` `--introY` |
| 全体の文字色 | `--i`（本文）`--im`（中間）`--il`（淡い）`--if`（最も淡い） |

### `book.v3.css` を触るもの

| 変えたいもの | 場所 |
|---|---|
| 見出し・リード・本文の文字サイズ | 「7. テーマ見開き（共通パーツ）」の `.kw` `.lead` `.bd` |
| 本文の置き場所（上寄せ／下寄せ） | `.bd.t`（上）`.bd.b`（下） |
| 左ページの情報ブロックの置き場所 | 「8. 各テーマ見開きの個別レイアウト」の `.k1L`〜`.k6L` |
| 目次の項目幅・高さ | `.ti` の `width`、`.tls` の `top` `height` |
| Instagramのカード | `.igg`（左グリッド）`.igf`（右の大判）`.igc` |

---

## 5. お問い合わせフォームの接続

フォームは**まだ送信先が未設定**です。設定するまで、送信を押すと「送信先が未設定です」と表示されます。

`book.v3.js` の一番下、「3. お問い合わせフォーム送信」の中を編集してください。

```js
var GAS_URL="";                 // ← Google Apps Script のURL（末尾 /exec）を貼る
var RECAPTCHA_KEY="";           // ← reCAPTCHA v3 のサイトキー。使わないなら空のまま
var TEL="0237-00-0000";         // ← エラー時に案内する電話番号
```

SP版で使っている `Code.gs` を**そのまま流用できます。** 新しく増えた「問い合わせ項目」は、送信時に本文の先頭へ

```
【お問い合わせ項目】宿泊のこと（嘉左衛門・平左衛門）

（お客さまの入力内容）
```

という形で差し込んでいます。GAS側の改修は不要です。

**項目の選択肢**は仮置きです。埋め込みコードの `<select id="cfK" name="topic">` の中の `<option>` を実際の分類に合わせて書き換えてください。

---

## 6. 設計データについて、確認をお願いしたい4点

デザインの文字をそのまま起こすと不自然な箇所がありました。判断して直したものは印をつけています。

| 場所 | デザインの表記 | 対応 |
|---|---|---|
| とまる／嘉左衛門 | 個室３室／大広間**１室室** | 「１室」に直しました |
| たべる／本文 | スイーツ**提供する**カフェ | 「スイーツ**を**提供する」に直しました |
| つくる／叶屋 | 「…のほか、**山で取れた**」で文が途切れている | 「山で取れた素材を使ったプロダクトを展開しています。」と仮に補いました。**正しい続きをお知らせください** |
| お問い合わせ／平左衛門 | 西川町**間沢117番地** | とまるページでは「大井沢」なので**大井沢を採用**しました |

あわせて、Instagramの見出しは手書き書体だと読み取りにくいのですが、PDFの文字データで「**日々のあれこれ**」と確定しています。

---

## 7. 設計と実装で1か所だけ変えている点

デザインでは、左ナビがある見開き（とまる以降）で**ノドの位置が右にずれています**（画面幅1400pxのうち742px＝53%の位置）。ナビの無い目次・Instagramでは50%です。

実装では**ノドを常に50%に固定**しました。理由は、めくりの回転軸がノドそのものだからです。ページが3％移動すると、めくっている最中に紙の幅が変わって歪みます。

見た目の差は画面幅の3%ほどで、ナビは左ページに重ねて置き、ページの中身はナビ幅ぶん内側から始まるようにしてあります。もしデザインどおりにずらしたい場合は相談してください（めくりの作り方を変える必要があります）。

---

## 8. うまくいかないときの見分け方

| 症状 | 原因 | 対処 |
|---|---|---|
| 文字が縦に積まれず横並びで崩れる | CSSが届いていない | `book.v3.css` のURLを直接開いて確認 |
| ローディングのまま進まない | JSが届いていない／GSAPが読めていない | `book.v3.js` のURLを直接開く。ブラウザの検証→Consoleでエラーを確認 |
| ナビを押しても動かない | v1/v2のJSが残っている | 埋め込みコードの読み込み先が `book.v3.js` になっているか確認 |
| スマホでPC版の本が出てSP版が隠れる | 埋め込みコードの `@media(max-width:768px){.m{display:none}}` が消えている | 768の値を、embed・`book.v3.js` の `NARROW()`・StudioのSP用Embed表示切替の3か所で揃える |
| ページ全体がスクロールしない（本の外も） | `html.bkl` の固定が効いたまま | 意図した動作。本を全画面にしない構成に変える日は該当行を消す |
| 大きな写真がノドで切れてつながらない | 左右どちらかの `--x1`〜`--y2` が違う | 左右2か所の数字を同じにする |
| 一部の見開きが飛ばされる | 紙のidが飛んでいる | 埋め込みコードで `id="p0"` 〜 `id="p8"` が9つ揃っているか確認 |
| Instagramが「ダミー」のまま | GASのフィードが止まっている | `book.v3.js` の `FEED_URL` を直接ブラウザで開いてJSONが返るか確認 |

---

## 9. いま公開中の `book.v2.js` について（要確認）

`https://tukinowa-git.github.io/tukimichi-test/book.v2.js` と `@v2.0.0` の実物を取得して中身を確認したところ、**スマホ幅ガード（`NARROW()`）が入っていない版**でした。9月4日のやりとりの後半で追加した⑤の修正が、アップロードに反映されていない可能性があります。

このままだとスマホでPC版の本がSP版を覆い隠します。v3では⑤を確実に入れてあり、ブラウザ幅390pxで「PC版が非表示・エンジン未起動・ページは普通にスクロールできる」ことを実機で確認済みです。**v3へ差し替えれば解消します**ので、v2側を直す必要はありません。

---

## 10. 次にやること

- [ ] 写真18か所の差し替え
- [ ] 叶屋の説明文の続きを確定
- [ ] お問い合わせ項目の選択肢を確定
- [ ] `GAS_URL` と `RECAPTCHA_KEY` の設定
- [ ] リンク先（`#stay-kazaemon` `#plan` `#kanaya` など仮のアンカー）を実URLへ
- [ ] SP版を10見開き構成に合わせて更新（SP用Embedボックスはそれまで残す）
- [ ] 公開前チェック（パスワード解除・noindex解除・サイトマップ有効化・Googleビジネスプロフィール作成）
