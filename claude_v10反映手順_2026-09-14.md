# v10 反映手順（2026-09-14）

## 1. 順番を守ってください

1. GitHub リポジトリ直下に **`book.v10.css`** と **`book.v10.js`** をアップロード
   - 画像の追加はありません。v9.0.0 のものをそのまま使います
2. Releases → Create a new release → タグ **`v10.0.0`** → Publish
3. jsDelivr で開けることを確認
   `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v10.0.0/book.v10.css`
4. STUDIO に2本とも貼り替えて公開
   - トップページ … `claude_PC版embedコード_v10_0_2026-09-14.html`
   - `/stay/kazaemon` … `claude_嘉左衛門ページembedコード_v10_0_2026-09-14.html`

**2 を飛ばして 4 をやると、タグが存在しないので全部出ません。**
トップと嘉左衛門は同じタグを見ているので、どちらか一方だけの貼り替えはしないでください。

STUDIO の3万字制限に対する余裕：トップ 1,701字 ／ 嘉左衛門 19,708字

---

## 2. Safari の残像について

### 原因
紙9枚はすべて同じ奥行き（z=0）に並んでいて、前後関係は `z-index` だけが頼りでした。
Chrome / Edge は `z-index` の変更をすぐ反映しますが、WebKit は
`will-change:transform` で常時レイヤ化された要素の並び替えを一拍遅れて適用します。
めくり終わって z が 0 に戻った瞬間に全部が同じ奥行きに戻り、そこで前の紙が上に残る——
「めくりの最中と、完了してから1秒ほど」という症状と一致します。

### 対策（book.v10.js ㉑㉒）
1. 紙ごとに **0.08px きざみの実際の奥行き**を与え、z-index に頼らず順序を確定
   （最大 0.72px／遠近法 2800px なので、大きさの変化は 0.03% 未満＝見えません）
2. `will-change:transform` を常時9枚ではなく、**いまめくっている紙だけ**に
   （画面半分×9枚ぶんの合成レイヤを抱え続けずに済みます）

### 他への影響
Chrome / Edge の見た目と動きは変わりません。手元の Chromium では
15通りの画面サイズ × 全10見開きで確認済みです。
**Safari は手元で動かせないので、公開後に実機で一度ご確認ください。**
もし残っていた場合、次に試す価値があるのは「めくり終わりの1フレームだけ
`.bk` に再描画を促す」対策です。

---

## 3. 目次の重なりについて

最長行24文字 × 1.04em = **24.96em** 必要なのに対し、`height:46%` では
フォントが `1.85vh` 側で効く画面比のとき **46.18%** 必要でした。0.18%の不足です。

実際に崩れていた解像度（実測）：**1600×900 / 1440×820 / 1366×768 / 1280×720**
1400×868 だけがギリギリ通っていたので気づきにくい状態でした。

`53%` に広げて全サイズ解消しています。文字は枠の上端から始まるので、
枠を広げても**見た目は変わりません**。

---

## 4. あわせて直したもの（ご指摘以外）

**1366×768 で、とまる・たべる・つくるの情報ブロックが枠から62〜95pxはみ出していました。**
縦が低いと説明文の段数が増えるためです。ご依頼の「段落間を空ける」は横幅を使うので、
先にこれを解決しないと成立しませんでした。

- `left:0`（＝これより左へは出ないという上限）に変更。中身は右づめで並ぶので、
  **ノド側の余白＝設計データどおりのまま**、段数が増えても収まります
- 枠の高さを増やし、1段に入る文字数を増やして段数そのものを減らしました

---

## 5. 段落間は設計データの実測値に合わせました

設計データを画素実測したところ、段間は一律ではなく意図的に差がついていました。

| | 設計実測 | v9 | v10 |
|---|---|---|---|
| 施設名 ↔ 間取り | 約30px | 6.4px | 約30px |
| 見出し群 ↔ 説明文 | 約15px | 8.8px | 約15px |
| ブロック同士 | 約50px | 23.8px | 約46px |

とまるの情報ブロックの右端は、設計 682px に対し v10 も **682px** で一致しました。

---

## 6. 嘉左衛門ページの表示速度

### 原因
画像が **CSS → このコード → やっと画像** という直列になっていました。
`data-i` に入れた画像パスを JavaScript で `src` に展開していたため、
render-blocking な CSS が届くまで、画像のダウンロードが始まりませんでした。
回線状況で「速い時と遅い時」が出ていたのはこれが主因です。

### 対策
1. **画像に `src` を直接記述**。ブラウザが HTML を読んだ時点で取りに行けます
   （副系への切り替えは各 `<img>` の `onerror` が担当。しくみは変わりません）
2. いちばん重い外観写真に `preload` + `fetchpriority="high"`
3. このページに「scroll」は無いので **Karla の読み込みを廃止**（1往復ぶん短縮）
4. 中身を出すまでの待ち受けを 2.5秒 → 1.4秒

なお、トップページから遷移した場合は CSS がキャッシュに乗っているので元から速く、
**直接アクセスや再読み込みのときに遅く感じていた**はずです。上の対策はどちらにも効きます。

---

## 7. 調整ノブ（STUDIO 側で上書きできます）

タグを切り直さずに STUDIO の `<style>` で変えられます。

```
.m{--fltH:47%;--fmkH:50%}      奥付のタイトルロゴ／ロゴマークの高さ
.m .ibs{--ibsg:46px}            情報ブロック同士のあき
.m .ib{--ibg:15px}              見出し群と説明文のあき
.m .ibm{--ibmg:24px}            施設名と間取りのあき
.m.solo .kzi{--kzhGap:20px}     「ゲストハウス」と本文のあき
.m.solo .kzi{--kzgGap:4px}      ◎のグループどうしのあき
```

奥付は、タイトルロゴを 39%→47% に拡大した分、ロゴマークを 57%→50% にしています。
マークは横幅がいっぱいまで来ていたので、縦の配分を振り替えるしかありませんでした。
マークを元の大きさに戻したい場合は `--fmkH:57%` と `--fltH:39%` に戻せます。

---

## 8. 嘉左衛門の情報欄に文言を足すとき

`<br>` が「桁の変わり目」です。**1桁に入るのは17字まで**なので、
どの行も17字以内で切ってあります。文言を足すときはこれを目安にしてください。
超えると、その行だけ自動で折り返して桁が1つ増えます。

---

## 9. 検証の範囲と、できていないこと

手元の Chromium で、下記15通り × 全10見開き＋嘉左衛門ページを自動チェックしました
（情報ブロックのはみ出し、目次の折り返しと重なり、Instagram見出しの重なり、
目次キャプションのはみ出し、奥付のはみ出し、嘉左衛門の折り返しと重なり）。**全て合格です。**

1920×1080 / 1680×1050 / 1600×900 / 1512×900 / 1440×900 / 1440×820 / 1400×868 /
1366×768 / 1280×800 / 1280×720 / 1180×820 / 1100×700 / 1024×640 / 900×600 / 820×560

**できていないこと：**
- **Safari と Firefox の実機確認**（コンテナから動かせません）。特に Safari の残像は
  対策の効果を確認いただきたい項目です
- 縦中横（`text-combine-upright`）の Safari での見え方。仕様上は対応していますが、
  `10台` `30分` `21時` の並びは実機で一度ご確認ください
- 820×560 のような極端に小さい窓では、嘉左衛門の情報欄の文字を 7.5px まで
  落として収めています。読めますが小さいです
