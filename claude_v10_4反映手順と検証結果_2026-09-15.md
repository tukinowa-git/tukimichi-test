# v10.4 反映手順と検証結果（2026-09-15）

## 1. 反映の順番

1. GitHub 直下に **`book.v10.4.css`** と **`book.v10.4.js`** をアップロード（画像の追加なし）
2. Releases → Create a new release → タグ **`v10.4.0`** → Publish
3. 開けることを確認
   `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v10.4.0/book.v10.4.css`
4. STUDIO に2本とも貼り替えて公開

STUDIO の3万字制限に対する余裕：トップ 918字 ／ 嘉左衛門 18,114字
（トップは余裕が少なくなってきたので、次回は古い変更履歴を削ります）

---

## 2. ローディングの透け — 原因が特定できました

### 原因：紙に下地の色が指定されていませんでした

```
.bc,.pf,.cl>*,.cr>*{background:var(--bg) center/cover no-repeat}
                                                  ↑ 背景色が無い
```

紙（`.pf`）の背景は **bg-paper.webp という画像だけ**で指定されていました。
この画像が届くまでの間、紙は**透明**です。紙は9枚重なっているので、
下の見開き8枚の中身がそのまま透けて見えていました。

初回だけ起きて2回目以降は起きないのは、**2回目は bg-paper.webp が
キャッシュにあって即座に描かれる**からです。ご報告の内容と完全に一致します。

遮蔽用の `#shield` も `.m.pre` も正しく効いていましたが、
どちらも「紙そのものが透明」という問題は防げませんでした。

### 対策：背景色を1つ足しました

```
.bc,.pf,.cl>*,.cr>*{background:var(--bg) center/cover no-repeat #faf6ef}
```

### 実証

bg-paper.webp をわざとブロックして、4秒後の画面を比べました。

| | 本文の透け量 |
|---|---|
| v10.3 | 207,751px（Instagramのカード・写真・他の見開きが透けて見える） |
| **v10.4** | **145,634px（ローディングの版画と文字だけ。透けなし）** |

差の 62,117px が、透けて見えていた分です。

---

## 3. その他4件

### ① 目次：見出しと本文のアキ

設計PDF実測で **22.9px** 必要なところ、v10.3 は **2px** しかありませんでした
（見出しと本文がほぼ接触）。`.hb` の `right` を 39.5% → 42.7% にして
**21px** にしています。「私たちのこと」のボタンとの間隔も12〜65px 確保しました。

### ② とまる：ノドからの余白

19px → **24〜42px**（画面サイズによる）。

**参考までに、設計PDFの実測値は約13pxで、v10.3 の19pxより狭いです。**
ご指示どおり広げましたが、設計より広くなっています。詰め直したい場合は
`.k1L .ibs{right:5%}` の数値を下げてください（3% で設計相当）。

### ③ たべる：写真のノドからの余白

15px → **22〜38px**。写真の幅を 2% だけ詰めています（左端は動かしていません）。

**こちらも設計PDFの実測値は10pxで、v10.3 の15pxより狭いです。**
同じく、ご指示を優先しました。

### ④ 嘉左衛門：級数の統一とリンクの下端

**級数** … v10.3 は桁数を守るために `--ibdFS` より小さくしていました。
枠の高さを **em 基準（17.8em＝17字ぶん）** にしたので、縮めなくても
17字入るようになり、`--kziFS:var(--ibdFS)` に戻せました。
1440×900 で 13.392px、トップの説明文と**完全に同じ**です。

**リンクの下端** … 枠の高さが本文の実寸と一致するようになったので、
`align-self:flex-end` の「宿泊プランを見る」の下端が本文の下端と
そろいます（実測で一致を確認）。

16:9 のような横長の画面では本文が縦に伸びるため、天を 69.3% → 66% に
上げる指定を1つ入れています（`min-aspect-ratio:17/10`）。

---

## 4. 検証

| 画面 | 目次アキ | とまるノド | たべるノド | 嘉左衛門の桁 | リンク下端 | 級数 |
|---|---|---|---|---|---|---|
| 1440×900 | 21px | 31px | 28px | 3/6/1/7 | 一致 | 13.39 |
| 1400×868 | 21px | 30px | 27px | 3/6/1/7 | 一致 | 13.02 |
| 1366×768 | 21px | 29px | 27px | 3/6/1/7 | 一致 | 12.70 |
| 1600×900 | 25px | 34px | 31px | 3/6/1/7 | 一致 | 14.50 |
| 1280×720 | 19px | 28px | 25px | 3/6/1/7 | 一致 | 11.90 |
| 1920×1080 | 48px | 42px | 38px | 3/6/1/7 | 一致 | 14.50 |
| 1100×700 | 17px | 24px | 22px | 3/6/1/7 | 一致 | 9.45 |

嘉左衛門の桁数は**全サイズで設計どおり 3/6/1/7（＝17桁）**。
リンクのクリック判定も **79個すべて反応**します。

1100×700 だけ級数が 9.45px（`--ibdFS` は 10.23px）です。小さい画面用の
規則が効いているためで、通常のノートPCでは一致します。

---

## 5. 実機で確認していただきたいこと

- **初回アクセスでのローディング**（スーパーリロード、または
  シークレットウィンドウで開くとキャッシュ無しの状態を再現できます）
- Safari での表示（めくりの対策はそのまま入っています）
- 目次・とまる・たべるの余白が意図どおりか

---

## 6. 文言を足すときの目安（嘉左衛門）

`<br>` が「桁の変わり目」です。1桁は **17字ぶん**で、◎グループは
先頭4字が字下げに使われるので**内容は1行13字**まで。
アクセスは字下げが1字なので**16字**までです。
