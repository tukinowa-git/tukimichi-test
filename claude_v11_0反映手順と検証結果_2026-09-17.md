# 月みちて、山となる。 v11.0 反映手順と検証結果（2026-09-17）

クライアント打ち合わせの修正指示（PC版・全10見開き＋嘉左衛門ページ）を反映した版です。
指示のない箇所は v10.4 から動かしていません（後述の「動いていないことの確認」参照）。

## 1. 納品物

| ファイル | 置き場所 | 内容 |
|---|---|---|
| `book.v11.css` | GitHub リポジトリ直下 | スタイル本体（v10.4 から修正箇所のみ変更） |
| `book.v11.js` | GitHub リポジトリ直下 | エンジン本体（変更はローディングの1か所のみ） |
| `claude_PC版embedコード_v11_0_2026-09-17.html` | STUDIO トップのカスタムコード | 28,483字（上限まで残り1,517字） |
| `claude_嘉左衛門ページembedコード_v11_0_2026-09-17.html` | STUDIO /stay/kazaemon のカスタムコード | 10,336字 |

画像の追加・差し替えはありません（img/ は v9.0.0 と同じもの）。

## 2. 反映手順（この順番を守ってください）

1. `book.v11.css` と `book.v11.js` をリポジトリ直下にアップロード
2. Releases → Create a new release → タグ `v11.0.0` → Publish
3. 次のURLが開けることを確認
   `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.0.0/book.v11.css`
   `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v11.0.0/book.v11.js`
4. STUDIO のトップと /stay/kazaemon の両方に新しい埋め込みコードを貼って公開

手順2を飛ばして4を行うと、タグが存在しないため何も表示されません。
元に戻すときは、v10.4 の埋め込みコード2本を貼り直すだけです（v10.4.0 のタグは残っています）。

## 3. 修正内容と根拠

設計データ（1400×868）と v10.4 の描画を画素単位で比べ、数値を決めました。

### 全ページ共通

**ノドの影**
中央の濃さを輝度 −18.4 → −10.9（約4割薄く）にしました。境目は見える範囲に残しています。
`.spn` .05→.03、`.pf:before` .04→.025。嘉左衛門ページも同じです。

**情報ブロックの字間と段落**
実測の結果、設計の説明文の字送りは v10.4 と同じ（13.5px）でした。
折り返しが設計とずれて見えていた原因は字間ではありません。原因は、文字の大きさが画面の「幅」で、枠が画面の「高さ」で決まっていたことです。
ノートPCでよくある横長のブラウザ窓では、1列が14〜15字（設計は16字）になっていました。
そこで次の3点を行いました。

- 字間をご指示どおり .04em → .02em に詰めた
- 枠の高さを「1列の字数（`--ibN`）×字送り」で決めるようにした（標準16字、しる：ワークショップ14字／体験ツアー・ガイド17字、ためす：12字／13字、すまう：15字）
- 説明文の大きさ `--ibdFS` に、画面の高さの上限（1.5vh）を加えた

これで、どの画面サイズでも設計と同じ位置で折り返します。いちばん長い列・リンク・間取りの下端も、1px以内でそろいます。

`--ibdFS` の変更による文字の大きさ（設計の比率より横長の画面だけ小さくなります）：

| 画面 | v10.4 | v11 |
|---|---|---|
| 1400×868（設計） | 13.02px | 13.02px |
| 1440×900 | 13.39px | 13.39px |
| 1512×870 | 14.06px | 13.05px |
| 1440×789 | 13.39px | 11.84px |
| 1366×768 | 12.70px | 11.52px |
| 1280×720 | 11.90px | 10.80px |
| 1920×1080 | 14.50px | 14.50px |

v10.4 の大きさに戻したい場合は、トップの埋め込みコードの `--ibdFS` の `1.5vh` を `1.85vh` にしてください。嘉左衛門ページも同じです。
ただしその場合、横長の画面では文字が枠からはみ出す、または折り返しが設計とずれます。

**施設名（嘉左衛門・平左衛門など全見開き）**
設計実測（19.5px・字送り23.3px）に合わせ、次のように変えました。

- 級数：17.5 → 19.5px
- 字間：.12 → .2em
- 天：6px 下がっていた原因（`margin-top:.35em`）を削除し、説明文と同じ高さに

住所（西川町間沢など）は v10.4 と同じ大きさ（設計実測 11.4px）のままです。嘉左衛門ページの「ゲストハウス」も同じ体裁です。

### 00 ローディング

`book.v10.4.js` の演出が、縦書きで字間を `0px` に上書きしていました。GSAP が em を換算できないためで、字間の動き自体も出ていませんでした。
この字間の指定だけを外し、CSS の字間 .27em（設計の字送り）が効くようにしました。フェードと浮き上がりの動きは同じです。

- 文字：16.5 → 18.1px（型合わせで設計と字の長さが一致した大きさ）
- 天：変更なし（209px）
- 横位置：右へ約21px（`--introX` 68% → 65%）

### 01 目次

「私たちのこと」の枠の下端を、本文の下端（見た目の最下端）にそろえました。
本文と同じ文字サイズの変数から計算するため、5つの画面サイズすべてで差は+1pxです。
v10.4 は設計サイズで本文より30px上で終わっていました。

### 02 Instagram

- カードを約8%縦長にしました（1400×868で 289.6 → 313.5px）。右の大きいカードも同じ比率です。
- 投稿間の余白は変わりません。行間は v10.4 の「枠の高さの6%」を同じpxの固定値（4.26vh）に置き換えました（37.1px → 37.0px、列間は29.0pxのまま）。
- 伸びた分は写真が使います。キャプションは常に4行分の高さを取るので、6枚の写真の高さがそろいます。v10.4 では 1440×789 で4行目が切れていましたが、それも解消しています。
- 「Instagram」のリンク先：`https://www.instagram.com/guesthouse_cafe.kazaemon`

### 03 とまる

- 間取りの文字を、説明文と同じ大きさ（設計実測 13px。v10.4 は 10.9px）にしました。つくるの「アパレル・テキスタイル」も同じ部品なので、同じ大きさになります。
- サイト上の表記はもともと「大広間１室」で、「室室」の重複はありませんでした。
- 施設名は「共通」の項のとおりです。

### 04 たべる

- 「営業日・メニューについては、」と読点を追加しました。
- 読点を足すと自動の折り返しで「公式」が列をまたぐため、設計どおり次の3列で改行を入れています。
  「営業日・メニューについては、／公式Instagramもしくは公式LINE／でご確認ください。」
- Instagram：`https://www.instagram.com/guesthouse_cafe.kazaemon`
- 公式LINE：`https://line.me/R/ti/p/@011lugif?oat_content=url&ts=09161611`

### 05 しる

- 体験プログラムのブロックを、詳細・リンクごと削除しました。
- 上下の写真は、どちらも高さ 16.71% → 28.685% に伸ばしました。間は他の見開きの写真の組と同じ 0.92%（約8px）です。上の写真の左端は下の写真に合わせました（1px の差をそろえた）。
- 「ツアーガイド」→「体験ツアー・ガイド」に改称しました。見開きの写真枠（撮影待ちのグレー表示）の名前も同じにしています。
- 1列17字にし、次の3列に収めました。
  「オーダーメイドでプランの提案をいた／します。お気軽にお問合せください。／◎料金　３，３００円／時間（税込）」
- 2つのブロックは地（下端）をそろえています。
- 公式instagram：`https://www.instagram.com/guesthouse_cafe.kazaemon`

### 06 ためす

- ◎の項目を4字ぶら下げにしました。折り返した列と※の列が、「◎料金　」の直後の高さから始まります（嘉左衛門ページと同じ組み方）。
- 括弧の途中で切れないよう、「／時間（税込）」「／（不定休）」「／（許可取得済）」などの位置で改行を入れています。
- チャレンジキッチンは1列12字にしました。ぶら下げで列が増え、11字のままだと左端がナビの下に入るためです。上下の写真の間の中央に置いています。
- シェアアトリエは v10.4 と同じ13字で、地をそろえています。
- シェアアトリエの公式instagram：`https://www.instagram.com/atelier_kanaya`

### 07 つくる

- 「sewing studio」→「creative studio」
- 説明文を差し替え、「…草木による染織など。」の後で改行しました。
- 紹介サイト：`https://faber-gassan.com`
- 説明文が2列増えたため、ノド側の余白を 9.5% → 5%（とまると同じ）にしました。そのままでは左端がナビに7px食い込むためです。ブロックの中の間隔は変えていません。

### 08 すまう

- 本文：「〜大切だと考えています。」
- 2つのブロックの右位置を 30.8% にそろえました（v10.4 は 31%／30.6%）。両ブロックとも1列15字・9列なので、左端（リンク）の位置も一致します。
- にしかわナリワイ留学の「詳細を見る」：`https://www.town.nishikawa.yamagata.jp/soshiki/tsunagu/10839.html`
- 移住体験コンシェルジュ：「西川町HP（にしかわぐらし）」（縦書きのため括弧は全角）
  リンク先：`https://www.town.nishikawa.yamagata.jp/site/iju/`

### 09 お問い合わせ

- ロゴマークを上、タイトルロゴを下にしました（並び順の入れ替え）。
- タイトルロゴは、枠の高さの 39% → 17.5%（今の45%）にしました。
- ロゴマークは、画像の左右にある透明の余白（約13%ずつ）を考慮して拡大しました。絵の幅は、1400×868 で 402 → 495px（+23%）、横長の画面では約+26%です。
- ロゴ・タイトル・運営者の2行の中心を、ページ本文領域の中心にそろえました。v10.4 はノド側だけに余白があり、中心が27px左に寄っていました。
- 奥付：「運営　ツキノワ合同会社／山形県西村山郡西川町間沢117番地」に変更し、中央揃えにしました。助成の文は左揃えのままです。

### 嘉左衛門ページ

情報欄は、写真を縮めずに下げました。

- 位置の決め方を「天」から「地」に変え、地を右下の丸ボタンの下端と同じ高さにしました。
- v10.4 は横長の画面（16:9 など）で天を 66% に上げていたため、写真との間が約25pxしかありませんでした。この指定は廃止しました。

| 画面 | v10.4 の天 | v11 の天 | 写真との間 |
|---|---|---|---|
| 1400×868 | 69.3% | 69.9% | 54 → 59px |
| 1512×870 | 66.0% | 69.9% | 25 → 59px |
| 1440×789 | 66.0% | 69.9% | 23 → 53px |
| 1366×768 | 66.0% | 69.9% | 22 → 52px |

1400×868 のように地がすでにボタンの高さにあった画面では、下がるのは約5pxです。
これ以上下げると、本文が右下のボタンより下にはみ出します。さらに下げたい場合は、嘉左衛門の埋め込みコードの `<style>` に `.m .kzi{bottom:2.5%}` のように書けば調整できます。
1列17字の組み方は変わりません。

## 4. 検証結果

ローカルに STUDIO と同じ条件（Typekit 適用・jsDelivr の差し替え）の描画環境を作り、Chrome で確認しました。

**段落の組み方**
次の11サイズで、03〜08 の全ブロックの折り返し位置が完全に一致しました（食い違い0件）。
1400×868／1512×870／1440×789／1366×768／1280×720／1920×1080／1920×960／1440×900／1100×700／2560×1300／1280×1024

**はみ出し**
- ナビとの間隔：最小13px（ためす・1280×1024）
- 写真との間隔：最小16px（ためす・1280×720）
- 地：92.8% 以内

**リンク**
2サイズ×9見開きの全リンク・ボタンを、実際にクリックできる位置か判定しました。不可は0件です。

**めくり**
ホイール・左ナビ・「ご利用はこちら」（たべる→ためす）とも正常です。

**動いていないことの確認**
指示のない要素が v10.4 から0.6px以上動いたものは、1400×868 と 1512×870 で0件でした。
対象は、見出し・リード・本文・写真・見開き写真・左ナビ・右下ボタン・目次・「日々のあれこれ」・お問い合わせのフォーム／地図／SNSアイコン／助成の文です。

## 5. 気づいた点（今回は変更していません）

- **目次の説明**：しるの説明が「ツアーガイドとワークショップ」のままです。「体験ツアー・ガイド」にそろえる場合はお知らせください。
- **奥付のSNSアイコン**：09 の左下のアイコン（Instagram／LINE）は、リンク先がまだ `instagram.com/`・`line.me/` のままです。
- **右ページの本文**：設計サイズでも1列13字（設計は14字）で折り返しています。枠に0.1pxだけ入りきらないためで、情報ブロックと同じ方法で設計どおりにできます。
- **しるの写真**：上の写真（パンづくりWS）は、引き続き撮影待ちの仮画像です。
