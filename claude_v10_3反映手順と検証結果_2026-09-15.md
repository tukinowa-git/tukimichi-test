# v10.3 反映手順と検証結果（2026-09-15）

## 0. タグについて

**`v10.3.0`** で用意しました。v10.2.0 をまだ切っていない場合でも、そのまま
v10.3.0 で進めていただくのが安全です（タグは切り直すと混乱のもとなので）。

---

## 1. 反映の順番

1. GitHub 直下に **`book.v10.3.css`** と **`book.v10.3.js`** をアップロード
   - 画像の追加はありません
2. Releases → Create a new release → タグ **`v10.3.0`** → Publish
3. 開けることを確認
   `https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v10.3.0/book.v10.3.css`
4. STUDIO に2本とも貼り替えて公開

**トップページの中身は v10.2 と同じです。** 変更したのは嘉左衛門の情報欄だけですが、
同じタグを見ているので2本セットで貼り替えてください。

STUDIO の3万字制限に対する余裕：トップ 1,267字 ／ 嘉左衛門 18,341字

---

## 2. 私の実装が間違っていた点

いただいた PDF から**文字の座標をそのまま取り出して**測り直しました。
その結果、段落の組み方そのものが違っていたことが分かりました。

### 設計（PDF実測）

```
ゲストハウス
◎客室　個室３室（定員２〜４名）      ← ラベルと内容が同じ行
　　　　大広間１室（定員４〜８名）    ← 4字下げ
　　　　一棟貸し可（定員１２名）
◎設備　トイレ・洗面・シャワー・
　　　　ミニキッチン・ランドリー
　　　　は共用
　　　　※一棟貸しの場合はチャレ
　　　　ンジキッチンも貸出し可
　　　　（有料）
◎住所　西川町間沢１１７番地
◎アクセス                            ← ラベルだけで1行
○お車でお越しの方                    ← 字下げなし
　山形自動車道・西川ＩＣから５分      ← 1字下げ
　駐車場10台（無料）
○公共交通でお越しの方
　ＪＲ寒河江駅よりバスで約30分
　⇒バス停「間沢」から徒歩８分
宿泊プランを見る
```

### v10.2 までの私の実装

「◎客室」を独立した桁にして、内容を隣の桁から始めていました。
**ラベルと内容が同じ行に入る**という前提が違っていたので、
段落の形も、字下げも、桁数もずれていました。

### 実測した数値

| 項目 | PDF実測 | v10.3 |
|---|---|---|
| 字送り（桁の間隔） | 27.0px（全列一定） | line-height 2.0 ＝ 26.1px |
| 1字の送り | 14.0px | letter-spacing .04em ＝ 13.9px |
| 折り返しの字下げ | 56px＝**4字ぶん** | padding 4em／text-indent −4em |
| ◎グループ間の余分なアキ | **0**（字送りは一定） | 0 |
| ゲストハウスの次だけ | ＋22px | clamp(9px,1.55vw,28px) |
| 本文の天地 | 70.1%〜95.9% | 69.3%〜95.8% |

**◎グループの間にアキを入れていたのも誤りでした。** 設計の字送りは
全19列にわたって 27px で完全に一定です。

---

## 3. 検証

### 桁数が設計と一致しました

| | 設計 | v10.3 |
|---|---|---|
| ゲストハウス | 1 | 1 |
| ◎客室 | 3 | 3 |
| ◎設備 | 6 | 6 |
| ◎住所 | 1 | 1 |
| ◎アクセス | 7 | 7 |
| 宿泊プランを見る | 1 | 1 |
| **合計** | **19** | **19** |

折り返しの位置（「トイレ・洗面・シャワー・」で切る、「チャレ｜ンジ」で切る）も
設計どおりです。これらは `<br>` で固定してあるので、環境によってずれません。

### 画面サイズを変えても崩れません

1440×900 ／ 1400×868 ／ 1600×900 ／ 1366×768 ／ 1280×720 ／ 1920×1080 ／
1100×700 ／ 900×600 ／ 820×560 の**全9サイズで本文17桁**、枠のはみ出しなし。

リンクのクリック判定も **79個すべて反応**します。

---

## 4. ひとつだけ設計と違うところ

**縦が低い画面では、情報欄の文字がトップページの説明文より少しだけ小さくなります。**

1桁に17字入れる必要があるのに対し、`--ibdFS` をそのまま使うと
1366×768 では15字しか入らず、行が勝手に折り返してしまうためです。
`--kziFS` の vh 上限を 1.45vh にして、桁数を守る側を優先しました。

```
--kziFS:clamp(10px,min(.93vw,1.45vh),14.5px)
```

設計サイズ（1440×900）では 13.05px で、設計の 13.5px とほぼ同じです。
文字の大きさを優先したい場合は、埋め込み側でこの1行を
`--kziFS:var(--ibdFS)` に戻せます（そのかわり縦の低い画面で折り返します）。

---

## 5. Safari 対策はそのまま入っています

`book.v10.3.js` の本文は `book.v10.2.js` と同一です。
めくり中、回転角90度を境に「手前を向いている面」だけを出す方式のままです。

---

## 6. 実機で確認していただきたいこと

- **Safari での残像**（今回の本命）
- **縦中横**（10台・30分・21時）の数字が横に並んで見えるか
- 嘉左衛門の情報欄の**字下げが4字ぶんに見えるか**

---

## 7. 文言を足すときの目安

`<br>` が「桁の変わり目」です。**1桁は17字ぶん**で、うち先頭4字は字下げに
使われるので、◎グループの内容は**1行あたり13字**が上限です。
アクセスは字下げが1字なので**16字**まで入ります。
