# v6.0 → v6.1 移行手順

2026-09-08 ／ 月みちて、山となる。
URLからハッシュ（#s3）を無くす対応

---

## 1. 何が変わるか

**変わるのはURLだけです。** 見た目も操作も一切変わりません。

| 場面 | v6.0（いま） | v6.1 |
|---|---|---|
| 初回アクセス | `/` | `/` |
| めくっている間 | `/` | `/` |
| 詳細ページから戻った直後 | **`/#s3`** | **`/`** |
| その後ほかの見開きへ | **`/#s3` のまま（古い番号）** | **`/`** |
| 詳細ページの左ナビから戻る | **`/#s5`** | **`/`** |

戻る動作・横スワイプ・ローディングを出さない挙動は、すべて v6.0 のままです。

---

## 2. 差し替えるもの

**新しいファイルは `book.v6.1.js` の1つだけです。**

| | 変更 | 理由 |
|---|---|---|
| `book.v6.1.js` | **新規追加** | 今回の変更はすべてここ |
| `book.v6.css` | **そのまま** | 中身が変わらないため。参照も `@v6.0.0` のまま据え置き（1年キャッシュが効いているので、変えないほうが速いままです） |
| `img/` の画像8点 | **そのまま** | |
| タグ | **`v6.1.0` を追加** | v6.0.0 も残します |
| STUDIOの埋め込み2ページ | **両方とも貼り替え** | |

貼り替え後の参照先はこうなります。

```
CSS : @v6.0.0/book.v6.css     ← 変わりません
JS  : @v6.1.0/book.v6.1.js    ← ここだけ新しく
画像 : @main/img/              ← 変わりません
```

---

## 3. 手順

### 手順1　GitHubに `book.v6.1.js` を追加

リポジトリ直下で **Add file** ▸ **Upload files** → `book.v6.1.js` をドロップ → **Commit changes**

既存のファイルは何も消しません。

### 手順2　タグ `v6.1.0` を作成

**必ず手順1のあとに行ってください。** タグは「その時点のリポジトリの写し」なので、先に作るとファイルが含まれません。

Releases ▸ **Draft a new release** ▸ Choose a tag に `v6.1.0` ▸「Create new tag: v6.1.0 on publish」▸ **Publish release**

### 手順3　確認

次のURLを開いて、JavaScriptの文字列が表示されれば成功です。

```
https://cdn.jsdelivr.net/gh/tukinowa-git/tukimichi-test@v6.1.0/book.v6.1.js
```

**開けない状態で貼り替えると本が表示されません。** ここが開ければ失敗しません。

### 手順4　STUDIOを貼り替え

- トップページ →`</body>`直前を `claude_PC版embedコード_v6.1_2026-09-08.html` で全置換
- `/stay/kazaemon` →`</body>`直前を `claude_嘉左衛門ページembedコード_v6.1_2026-09-08.html` で全置換

**嘉左衛門ページも必ず貼り替えてください。** 左ナビの動きを変えているためです。SP用Embedボックスは触りません。

---

## 4. 公開後の確認

- [ ] 初回アクセスでローディング演出が出る
- [ ] **めくってもアドレスバーが `/` のまま**
- [ ] とまる →「詳細を見る」→ **戻るボタン** → とまるに戻り、**URLが `/`**
- [ ] そのまま他の見開きへ移動しても **URLが `/` のまま**
- [ ] 詳細ページの左ナビ（たべる等）を押すと、その見開きが開き **URLが `/`**
- [ ] 2本指の横スワイプでも戻れる
- [ ] リロードするとローディング演出が出る（従来どおり）
- [ ] 画像がすべて出ている

---

## 5. なぜURLを使わずに済むのか

v6.0では「どの見開きにいたか」をURLに書いて、戻ったときに読み取っていました。URLは戻る操作で必ず復元されるので確実ですが、そのぶん残り続けます。

v6.1ではURLの代わりに次の2つを使います。

**① sessionStorage** — 同じタブの中だけで生きる控えです。めくるたびに見開き番号を記録します。タブを閉じれば消え、別のタブにも影響しません。

**② navigation.type** — 「このページにどうやって来たか」をブラウザが教えてくれる仕組みです。

```
初回アクセス   → navigate
リロード       → reload
戻る／進む     → back_forward   ← これのときだけ①を読む
```

**「戻ってきた」ことをブラウザ自身が教えてくれるので、URLに印を残す必要がありません。** 初回アクセスとリロードでは①を読まないので、ローディング演出は従来どおり出ます。

詳細ページの左ナビだけは「進む」方向の移動なので②が使えません。こちらは「たべるを開いて」という一回限りの指示を①に置いてから `/` へ移動します。指示は読んだ時点で消え、30秒で期限切れになります。

**古い形式のURL（`/#s4`）で開かれた場合も従来どおり動きます。** v6.0の時期にブックマークされたURLや、⌘クリックで新しいタブに開いた場合のためです。ただし開いたあとURLからは消して `/` に戻します。

sessionStorageが使えない環境（プライベートモードの一部設定）では、v6.0と同じくURLに印を残す動きに自動で切り替わります。

---

## 6. 検証の記録

### 描画への影響

同一条件で全10見開きをレンダリングし、3回分を突き合わせました。

| 比較 | 差分画素 |
|---|---:|
| **v6.0（1回目） vs v6.1（2回目）** | **0（全10見開き完全一致）** |
| v6.1（1回目） vs v6.1（2回目） | 48,003（見開き02のみ） |
| v6.0（1回目） vs v6.1（1回目） | 48,003（見開き02のみ） |

見開き02（日々のあれこれ）の差は、**同じv6.1同士の比較でも同じだけ出ます。** Instagram欄の描画が実行ごとに微妙に揺れるためで、版の違いによるものではありません。平均差は0.65/255で目視できない水準です。

**v6.1 は描画を一切変えていません。**

### 動作の確認

```
1  初回はローディング表示           True
2  めくった後のURL               /
4  戻った先のURL                /       ← v6.0では /#s3 でした
5  とまるが開いている              True
6  ローディングなし                True
7  ためすへ移動後のURL             /       ← v6.0では /#s3 のままでした
8  ためすが開いている              True
9  ナビ「しる」後のURL             /       ← v6.0では /#s5 でした
10 しるが開いている               True
11 ローディングなし                True
12 リロードはローディング            True
JSエラー                        なし
```

### 旧形式URLとの互換

```
/#s4 を直接開いたとき
  たべるが開く      True
  ローディングなし   True
  URLは掃除される   /
```

---

## 7. 元に戻す方法

STUDIOのカスタムコードを v6.0 のものに戻すだけです。GitHubも画像も触りません。

---

## 8. 変更の中身

### book.v6.1.js

- **⑫' URLを変えずに戻り先を覚える**
  - `n()`（見開きが変わるたびに呼ばれる関数）で `sessionStorage` に番号を控える
  - 起動時に `navigation.type === "back_forward"` のときだけ控えを読む
  - `leave()` からURL書き込みを削除。sessionStorageが使えない場合のみ従来動作
  - `/#sN` での起動は従来どおり受け付け、読んだあとURLから消す

v6.0 の①〜⑭のしくみはすべてそのまま残しています。

### 嘉左衛門ページの埋め込み

- 左ナビと右下ボタンに `data-p` を付与
- クリック時に「どの見開きを開くか」を sessionStorage に置き、`/` へ移動
- `href="/#s4"` は残置。⌘クリックで新しいタブに開いたときはそちらが使われます

### book.v6.css

- **変更なし。** 再アップロードも参照の書き換えも不要です

---

## 9. 残っている課題

- 嘉左衛門ページの実素材（外観写真・間取り図×2・紋）
- 情報欄の改行位置を設計どおりに揃える（文言確定後）
- 題字を文字のままにするか、ロゴタイプ画像にするか
- 平左衛門ページ
- スマホ対応（Phase 4）
