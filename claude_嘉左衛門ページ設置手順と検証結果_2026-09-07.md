# 嘉左衛門ページ 設置手順と検証結果

2026-09-07 ／ 月みちて、山となる。 ／ `/stay/kazaemon`
第2段階（詳細ページ本体）

---

## 1. ファイル

| ファイル | 文字数 | 置き場所 |
|---|---:|---|
| `stay.v5.css` | 6,217 | GitHub `tukinowa-git/tukimichi-test` |
| `claude_嘉左衛門ページembedコード_v5_2026-09-07.html` | 8,337 | STUDIO `/stay/kazaemon` のカスタムコード欄（上限30,000／残り21,663） |

**トップ側のファイル（`book.v5.css` / `book.v5.js` / トップの埋め込みコード）は一切変更していません。** 第1段階で確認いただいた状態のままです。

---

## 2. 設置手順

### 手順1：GitHubに `stay.v5.css` を追加

既存ファイルは触りません。追加だけです。

### 手順2：リリースタグ `v5.1.0` を作成

`v5.0.0` はそのまま残してください。`v5.1.0` は `book.v5.*` と `stay.v5.css` の両方を含みます。

- トップページの埋め込みコードは `@v5.0.0` を見たまま → **書き換え不要**
- 嘉左衛門ページの埋め込みコードは `@v5.1.0` を見ます

### 手順3：STUDIOで `/stay` を作成

**先にこちらを作ってください。** STUDIOのディレクトリはパス文字列から自動生成されるだけなので、`/stay` というパスのページが実在しないと `/stay` は404になります。パンくずの親が404を指すのは避けたいので、簡素な一覧ページで構いません。

### 手順4：STUDIOで `/stay/kazaemon` を作成

ページ設定 → パスに `stay/kazaemon` と入力。カスタムコード →`</body>`直前に埋め込みコードを全文貼り付け。

### 手順5：ページ設定

| 項目 | 内容 |
|---|---|
| タイトル | 嘉左衛門｜guesthouse & cafe｜月みちて、山となる。 |
| ディスクリプション | 山形県西川町間沢、旧六十里越街道にたつ築180年超の古民家をリノベーションしたゲストハウス＆カフェ。個室3室・大広間1室、一棟貸しも可能です。 |
| OGP画像 | 外観写真（1200×630）※素材が届いてから |
| noindex／パスワード | サイト全体の設定を継承。公開時に解除 |

**JSON-LD**（ページ設定のカスタムコード`<head>`へ）は、実素材と料金の扱いが決まってから入れることを勧めます。表示していない事実を書くとかえって不利になるためです。入れる場合は `LodgingBusiness` と `BreadcrumbList` の2つ。

---

## 3. 公開後に見ていただきたいこと

- [ ] 左ナビの6項目を押すと、トップの該当見開きが**ローディングなしで**開く
- [ ] 右下のメールボタン → トップのお問い合わせ、ホームボタン → トップの目次
- [ ] トップの とまる →「詳細を見る」→ このページ、で**紙色の点滅が起きない**
- [ ] 往復しても左ナビ・右下ボタン・背景・写真の枠が動かない
- [ ] 「宿泊プランを見る」が別タブでタビチャットを開く
- [ ] ブラウザの戻るボタンで戻っても画面が白くならない
- [ ] 点線の枠が4箇所（間取り図×2・紋・見開き写真のラベル）出ている

---

## 4. 素材の差し替え方

差し替えるのは4点です。いずれも`tmpb`（点線の枠）または`tmp`（グレー処理）を外すだけです。

| 箇所 | いまの状態 | 差し替え方 |
|---|---|---|
| 見開きの外観写真 | とまる見開きの写真を仮置き（グレー処理＋ラベル） | `class="xs tmp"` → `class="xs"`、`data-lbl` を削除、`data-i` を新しいファイル名に。**左右2箇所とも同じファイル名にすること** |
| 2F 間取り図 | 点線の枠 | `<div class="kzfp f2 tmpb" data-lbl="2F 間取り図"></div>` → `<div class="kzfp f2"><img data-i="ファイル名" alt="嘉左衛門 2階間取り図"></div>` |
| 1F 間取り図 | 同上 | 同じ要領で `f1` の行を |
| 紋 | 点線の枠 | `<div class="kzem tmpb" data-lbl="紋"></div>` → `<div class="kzem"><img data-i="ファイル名" alt=""></div>` |

**間取り図の画像には「2F」「1F」の文字を含めないでください。** そのラベルはHTMLの文字として別に置いてあります（設計の位置に合わせて実測済み）。

**見開き写真の矩形（`--x1:41.21;--x2:85.57;--y1:7.72;--y2:63.13`）は絶対に変えないでください。** この4つの数値が とまる見開きと一致していることが、遷移で額縁が動かない理由です。

**間取り図はSVGでいただけると最善です。** ラスタ画像だと室名（大広間・ラウンジ・ギャラリーなど）が検索にもAIにも読まれません。ラスタになる場合は、同じ内容をテキストで補う設計に変えます。

---

## 5. 検証の記録

Playwright + Chromium 141、1400×868で描画し、設計データ（1400×868）と画素単位で突き合わせました。

### 設計との一致

| 要素 | 設計 | 実装 | 差 |
|---|---|---|---|
| 2F 間取り枠 | x201-471 y105-259 | x201-471 y105-259 | **0px** |
| 1F 間取り枠 | x177-504 y323-494 | x177-504 y323-494 | **0px** |
| 紋 | x1271-1311 y257-295 | x1270-1310 y257-295 | 1px |
| 題字「嘉左衛門」 | x1272-1311 y309-466 | x1272-1311 y309-465 | 1px |
| guesthouse & cafe | y329-455（127px） | y329-456（127px） | 1px |
| 情報欄の枠 | x152-687 y592-815 | x152-687 y592-816 | 1px |

### トップページとの一致（遷移が成立する条件）

額縁にあたる要素が、トップの とまる見開きと**同一の位置**で描画されることを確認しました。ここがずれると遷移で画面が跳ねます。

| 要素 | とまる見開き（実装） | 嘉左衛門（実装） |
|---|---|---|
| 左ナビ | x26-95 y0-867 | **同一** |
| 右下ボタン | x1247-1357 y788-832 | **同一** |
| 本文 `.bd.b` | x847-1198 y592-809 | **同一** |
| 見開き画像の矩形 | --x1:41.21 --x2:85.57 --y1:7.72 --y2:63.13 | **同一** |

### 往復の動作

トップ →「詳細を見る」→ 嘉左衛門 → 左ナビ「とまる」→ トップ、を通しで実行。

```
① 詳細ページへ遷移           成功
② 到着直後のクラス           m solo hn hcn（正常）
③ トップへ戻る               成功（/#s3）
④ 復帰後                    m hn hcn
⑤ とまる見開きが開いている      true
⑥ ローディングを出していない     true
JSエラー                     なし
```

---

## 6. 作業中に判明した技術的な点

**縦書きの中では `flex-direction:row` が縦方向に走ります。**
`writing-mode:vertical-rl` を指定した要素では、インライン軸が縦になるため、`row` は上下、`column` が右→左になります。最初これに気づかず、見出しと本文が縦に積まれて1行の長さが167pxに削られ、桁数が設計の倍近くになっていました。`stay.v5.css` の該当箇所にコメントを残しています。

**字間を入れると桁数が変わります。**
情報欄は1行が桁の高さ（223px）に収まることを前提に桁数が決まります。`letter-spacing` を入れると「山形自動車道・西川ICから5分」などが溢れて桁が1本増え、全体が枠から食み出します。`letter-spacing:0` は必須です。

**縦書きの数字は全角にしないと横倒しになります。**
トップページの「個室３室／大広間１室」と同じ流儀に揃え、行長に余裕のある箇所を全角にしました。ＩＣ・ＪＲを含む行は全角にすると223pxを超えるため半角のままです。

---

## 7. 確認・判断をお願いしたい点

**① 情報欄の改行位置**
設計では「トイレ・洗面・シャワー・／ミニキッチン・ランドリー／は共用」のように、桁の切れ目が手で決められています。実装は自然な折り返しに任せているため、◎設備が設計の6桁に対して4桁になっています（全体の枠と桁ピッチ24pxは一致）。設計どおりに揃えることは可能ですが、**文言が確定してからのほうが手戻りがありません。** 実テキストをいただいた段階で合わせます。

**② 題字を文字にするか画像にするか**
いまはHTMLの文字（`<h1>嘉左衛門</h1>`）です。検索とAIに読まれる点で有利ですが、設計のロゴタイプが特定の書体であれば画像に差し替える必要があります。画像にする場合は読み上げ用の見出しを別に置きます。

**③ 文言の確認**
設計データから読み取ったものです。誤りがあればご指摘ください。
- 「JR寒河江駅よりバスで**約**30分」の「約」
- 「**⇒**バス停「間沢」から徒歩8分」の矢印の種類
- 「一棟貸し可（定員12名）」

**④ スマホ表示**
768px以下では本が非表示になるため、真っ白を避けて「PC表示のみの対応です」という案内を出しています。Phase 4に移る際は `stay.v5.css` の該当ブロックごと削除してください。

---

## 8. 平左衛門ページの作り方

デザインが出来たら、この型の複製で半日程度です。

1. `/stay/heizaemon` を作成し、嘉左衛門の埋め込みコードを複製
2. 文言・画像・タビチャットのリンク先を差し替え
3. 左ナビの `.ni.on` は「とまる」のままで正しい
4. 座標が違う場合のみ `stay.v6.css` を作る（`stay.v5.css` は消さない）
5. トップの埋め込みコードで、平左衛門の「詳細を見る」のコメントを外す

**別ページで作ってください。** 住所が違う以上、構造化データもGoogleビジネスプロフィールも棟ごとに分かれるためです。同じ棟の中で見開きを増やすときだけ、めくりを入れる判断になります。
